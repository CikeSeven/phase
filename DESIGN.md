# DESIGN.md — 相月 UI 规范

**月色玻璃，留白优先。** 以 Material 3 Expressive 为设计语言，保留月白/墨蓝画布、克制的彩色表面与局部玻璃质感，面向 Android 单手操作和长时间阅读。

本文只规定视觉、交互与 UI 验收要求，不记录实现方式或完成情况。产品功能见[产品设计](./docs/product_and_technical_design.md)，工程约定见 [AGENTS.md](./AGENTS.md)。

## 1. 设计原则

- **内容优先**：聊天突出阅读与输入，设置突出选择与操作；装饰、品牌和统计不抢占主任务。
- **层级克制**：先用留白，再用色面，最后用线条；避免每项一张描边卡片、框内套框和彩虹配色。
- **一致但不机械**：同类控件统一形状、反馈和语义，不把所有区域都做成玻璃卡片或胶囊。
- **状态真实**：明确区分加载、空数据、成功、失败、保存中和禁用；不显示无效入口或无依据的统计。
- **操作可预期**：选择、取消、返回和保存的含义保持一致，不因视觉调整改变用户选择或操作结果。

## 2. 配色基线

品牌靛蓝基准为 `#3D5A98`；浅色画布为 `#F5F7FC`，深色画布为 `#0C1423`。

| 色彩角色 | 用途 |
| --- | --- |
| 靛蓝 | 主操作、选中状态、输入焦点 |
| 青玉 | 连接、服务商与 AI 辅助标识 |
| 雾紫 | 思考、外观与辅助分组 |
| 月华金 | 少量月标、置顶等点缀 |
| 错误色 | 失败、删除与危险操作 |

- 前景与背景成对设计，正文、次要文字和彩色容器各有清晰层级；不为单个页面另造一套近似配色。
- 同一页面最多两种主要辅助色面，小面积金色点缀不计入。
- 深浅主题分别设计和验收，不机械反相，也不直接复用透明度而忽略合成效果。

## 3. 背景、表面与线条

- 背景采用宽幅、低饱和色域，不使用装饰光球或逐卡渐变；背景装饰不承担操作或信息表达。
- 玻璃仅用于需要层次的局部区域，模糊范围受限；长列表和连续阅读区域避免重复模糊。浮层底色应足够实，保证文字可读。
- 卡片用于真正相关的内容分组；普通列表允许无框、透明或直接铺在画布上。
- 边框和阴影只用于必要的边界与层次，不重复叠加外框、行框和分隔线；装饰图标与信息标签不得伪装成按钮。

## 4. 排版、间距与形状

| 项目 | 基线 |
| --- | --- |
| 间距 | 4dp 网格；常用 4 / 8 / 12 / 16 / 24 / 32 / 40 / 48dp |
| 页面留白 | 水平 16–24dp；分区 24–40dp；相关字段 16dp；相邻卡片 12dp |
| 圆角 | 8 / 12 / 16dp 用于紧凑控件及按压态；20dp 用于字段和选择面；28 / 32dp 用于较大表面 |
| 标题 | 页面 22sp / 1.25 行高；分区 16sp / 1.4 行高；用适度字重区分层级 |
| 正文 | 16sp / 1.5 行高；说明 14sp / 1.5；元信息 12sp / 1.45 |
| 触区 | 主操作最小高 56dp；其他可点击目标至少 48×48dp；图标通常 20–24dp |
| 内容限宽 | 设置、表单和底部面板 720dp；聊天阅读与输入 840dp；对话框 440dp |

- 字体跟随系统缩放，行高随内容增长；长名称允许换行或省略，但不得挤掉关键操作、遮挡浮动标签或截断必要信息。
- 主操作按钮采用胶囊轮廓，按压时适度收敛；字段、卡片和浮层保持各自的形状层级。
- 文本起点、图标和尾部操作对齐；尺寸遵循所在容器的可用空间，不按整屏宽度推算局部内容。

## 5. 控件、反馈与状态

- 主次操作通过填充、色面和留白区分；可点击区域必须有按压与焦点反馈，危险操作使用错误语义色。
- 选择面与下拉选项使用 Expressive 反馈：未选、选中、按压圆角分别以 20 / 28 / 12dp 为基准；选中项使用浅主色面和实心勾，并预留标记空间。
- 短选项使用锚定字段的下拉菜单，短操作使用锚定条目的菜单或按内容展开的底部面板；长列表使用可搜索的选择面板。锚定菜单圆角以 28dp 为基准，操作项从 20dp 收敛至按压态 12dp，操作不显示选择标记。菜单须避让屏幕边缘，选项支持大字换行；禁用时不可开启或提交选择。
- 锚定菜单启用可反向的展开与收起动效，减少动画时直接切换；快速重复开关不得留下遮罩或多余的返回层级。
- 动效短促、可中断、可反向；选择面形状可有轻微弹性，颜色以约 200ms 无回弹过渡。不得为播放动画延迟操作、移动相邻内容或缩小触区。
- 图标采用统一风格；纯图标操作提供明确的无障碍名称，状态变化同步更新标识。
- 局部加载或失败不遮挡无关操作；错误就近展示并提供恢复入口，不把读取失败当作空数据。

## 6. 页面与导航

- 页面转场遵循 Android 导航习惯；预测返回支持手势进度、取消与提交，侧栏拖动连续跟手并可反向。
- 返回先关闭最上层菜单、弹层或侧栏，再离开页面；点击菜单外只关闭菜单，不触发下层操作。
- 页面与弹层往返保留合理的滚动位置、搜索条件和未提交草稿，不主动抢焦点或弹出键盘。

页面交互要求：

- **聊天**：用户消息与 AI 回答层级清晰，正文优先，思考和工具信息独立呈现；长内容可阅读、复制和展开。接近底部时跟随新内容，回看历史时不抢滚动；展开、折叠保留阅读位置。
- **思考区**：默认收起为紧凑摘要行，手动展开后保留当前阅读选择；重新进入会话仍默认收起。耗时按各段实际接收阶段展示，无计时依据时不推算；正文或工具输出开始后停止该段计时。
- **输入与任务**：发送和停止入口位置稳定、状态可辨；发送未被接受时保留草稿。运行状态和停止入口不因切换会话而丢失，相关任务与结果可回到原会话查看。
- **会话侧栏**：新建、搜索、会话列表和设置层级清晰；行操作紧凑，更多菜单锚定对应条目，离开后返回保留侧栏状态。
- **设置与表单**：突出当前值、可操作项和保存入口，删除重复说明与装饰性统计；保存中防重复提交，失败保留草稿。取消不保存，也不因切换无关字段覆盖已有选择。
- **选择与名单**：当前选择明确；搜索、筛选和排序不改变勾选内容。黑白名单清楚说明勾选含义，数量、排序方向和大小等指标不得产生歧义；确认前只修改草稿。应用名单底部面板的应用类型与排序并排，长选项随字号换行，不省略排序方向。获取应用列表未授权或系统仅返回自身时明确提示访问受限，提供授权与重试入口，不能作为正常名单展示或确认；从权限设置返回后刷新，不改变名单草稿。
- **执行与结果**：执行与权限页只展示任务通知、无障碍服务和应用名单，不展示截图观察说明或文件/目录授权区域；权限用文字、图标与色面明确标识已授权和未授权，读取中、读取失败不显示为未授权。启动时仅对系统实际提供的应用列表运行时权限发起申请，拒绝后不在前后台切换中反复弹窗。权限、能力和执行结果分别表达，不把“权限已开”当作“功能已验证”。确认前展示目标与动作，结果使用具体文件、操作和实际状态说明，不向用户暴露内部协议或要求补填系统可观察的信息。

## 7. 弹层、键盘与文案

- 短确认使用紧凑对话框，短操作菜单按内容高度展开；搜索和长列表使用较高的底部面板，常规高度不超过可用空间的 85%。
- 底部面板保留标题、关闭入口和拖柄；内容可滚动，确认操作始终可达。未提交时支持返回、遮罩和下滑取消；保存中防止重复提交和误离开。
- 工具确认展示名称、动作摘要、目标、必要参数与剩余时间；停止、拒绝、允许一次三个动作分别清晰可达，关闭不视为批准。
- 键盘避让不重复，保留底部安全区；短屏、横屏和大字下允许内容与操作滚动到达，不靠截断或缩小文字塞满屏幕。
- 文案简洁、准确、可操作；保留字段含义、错误原因、删除影响和必要安全提示，删掉教学式注释、重复口号及复述按钮用途的说明。
- 成功与失败反馈不打断主要操作；不暴露密钥、鉴权信息、异常堆栈或原始响应。破坏性操作说明后果，失败不伪装成成功。

## 8. 无障碍与性能

- 正文与背景的最终合成对比度至少 4.5:1，不能以过低透明度制造“轻盈”；焦点、选中和错误同时具备非颜色提示。
- 读屏可识别控件名称、当前值、选中与禁用状态；装饰不重复朗读。下拉支持方向键与取消操作，不唤起输入键盘。
- 尊重减少动态效果的系统偏好：关闭非必要动画与模糊，保留静态状态反馈；离屏不持续播放动画。
- 长列表、流式内容和玻璃表面不得妨碍输入、滚动与手势响应；性能以目标设备上的实际帧数据判断，不以截图或主观观感替代。

## 9. 验收要求

- **布局**：检查 320/360dp 窄屏、横屏、1.3x/2x 字号、键盘、长文本与大量列表；无溢出、重要内容截断或不可达操作。
- **视觉**：深浅主题分别检查颜色层级、合成对比度、触区、形状和对齐；查看实际渲染结果，而非只确认截图已生成。
- **交互**：覆盖打开、选择、取消、提交、禁用、加载、失败重试与快速重复操作；确认未提交选择和阅读位置不会被意外覆盖。
- **导航与辅助操作**：验证返回预览的取消与提交、逐层关闭、侧栏跟手，以及读屏、键盘和减少动画偏好。
- **性能**：在目标 Android 设备的相同条件下比较滚动、拖动、转场与流式更新的帧耗时；区分自动化检查、视觉验收和真机测量，不将一种结果代替另一种。
