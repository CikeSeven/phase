# DESIGN.md — 相月 UI 规范

> 视觉方向：**月色玻璃**。基于 Material 3，面向 Android 的单手操作与长时间阅读。
> 工程约定见 [AGENTS.md](./AGENTS.md)。主题在 `lib/core/theme/`，共享界面组件在 `lib/core/widgets/`。

## 1. 设计方向

- **内容优先，操作有序**：聊天、模型选择、连接设置各有明确的主操作，不将保存按钮埋在长列表底部。
- **通透但清晰**：用有底色的半透明表面、细边界和少量背景模糊形成层次，禁止直接把文字透明度调低来制造玻璃感。
- **有色彩而不杂乱**：靛蓝为主，青玉、雾紫、月华金承担不同用途；不用彩虹横条、光球或每张卡片一种渐变。
- **留白与对齐**：标题、说明、字段、辅助操作构成清晰的阅读顺序；不再把所有内容套成大胶囊。
- **深浅色一起设计**：浅色是带冷色调的月白，深色是墨蓝；不能简单反相或复用一套透明度。

## 2. 主题与色彩

主种子色保持 `#3D5A98`，由 `ColorScheme.fromSeed()` 构造 Material 语义色。品牌扩展由 `BrandColors` 统一定义：

| 色彩 | 用途 |
| --- | --- |
| 靛蓝 / `ColorScheme.primary` | 主要按钮、当前选中项、用户消息 |
| 青玉 / `BrandColors.teal` | 服务商、模型、连接功能的图标与浅色衬底 |
| 雾紫 / `BrandColors.lavender` | 思考区、主题外观、次级信息 |
| 月华金 / `BrandColors.gold` | 月标、置顶等小面积品牌点缀 |
| `ColorScheme.error` | 失败、删除确认；不使用品牌色代替错误语义 |

每种辅助色提供深浅主题的前景、容器与容器文字配对。功能页面最多突出两种辅助色，正文保持 `onSurface`，说明使用 `onSurfaceVariant`；不能只用颜色表达状态。

业务组件经 `Theme.of(context).colorScheme` 或 `context.brandColors` 取色，不定义十六进制颜色。颜色字面量只允许存在于主题和代码高亮色表。

## 3. 背景与表面

- `AppBackground` 提供贯穿页面的月白/墨蓝底和宽幅、低饱和的青玉/雾紫色域，不使用离散装饰光球；装饰不接收手势或语义焦点。
- `FrostedSurface` 统一裁剪、半透明填色、细边框和 Material 点击反馈。禁止在一块玻璃内反复叠加多层白色/灰色背景。
- 玻璃层用于顶栏、侧栏、输入栏、固定操作栏和弹层；静态分区/列表卡片使用同族透明色面，关闭模糊。
- 浅色玻璃基底通常保留约 72–88% 不透明度，深色约 78–92%；弹层需更实，内容不可被底下的文字干扰。透明度以最终合成后的可读性为准。
- 模糊强度以 12 为基准、局部裁剪；不在消息或模型列表的每一行创建 `BackdropFilter`。
- 减少动态效果时禁用模糊与非必要动画，并使用足够实的 tonal surface；闪烁光标降级为可见的静态光标。
- 不用浓重阴影制造层级；弹窗、底部面板、悬浮按钮可以使用轻微 elevation。

## 4. 排版、间距与形状

- 字体跟随 Android 系统，不添加字体依赖。所有档位从 `TextTheme` 取用：页面标题 `titleLarge`、分区标题 `titleMedium`、聊天正文 `bodyLarge`（16sp / 1.5）、说明 `bodyMedium`、元信息 `bodySmall`。
- 页面水平留白 16–24dp，分区间距 24dp，卡片间距 12dp，关联字段间距 16dp，弹窗选项之间至少 8dp。间距统一来自 `AppSpacing` 的 4dp 网格。
- 圆角集中在 `AppRadius`：small 12（徽章/小控件），medium 20（字段/卡片），large 28（玻璃面板/消息），extraLarge 32（侧栏/弹层），full 40（短胶囊）。不让固定大圆角吞掉短行内容的可用空间。
- 内容在宽屏居中限宽：设置/表单约 720dp，聊天阅读区约 840dp，弹窗不超过 440dp。手机上遵循实际父布局宽度，不能把整个屏宽当作所有子组件的宽度。
- 长模型名、URL、会话名必须有截断/换行策略；不与开关、尾部按钮争抢同一行的固定宽度。

## 5. 共享组件

`lib/core/widgets/` 仅放真正跨页面复用的界面构件：背景/页面壳、图标徽章、分区标题、玻璃卡片、状态页、Dialog、底部操作栏。业务状态和数据库访问留在 feature 层。

- `ListTileTheme` 不再全局提供卡片背景；页面通过明确的容器决定是否分组，弹窗选项不会意外继承“粘连卡片”。
- 图标徽章按功能使用靛蓝、青玉、雾紫、金色浅底；可点击容器必须有 Material 反馈。
- 共享状态页包含标题、说明和真实可执行的操作，能够在小高度下滚动，不依赖固定居中 Column。
- 未实现的能力明确标注“即将上线”，不显示伪造助手、连接成功、用量或历史记录。

## 6. 页面布局

### 聊天

- 顶栏保留侧栏按钮，当前服务商/模型作为有下拉提示的可点击入口；新会话是独立小操作。设置不回到右上角。
- 空状态展示品牌、简短引导与现有功能入口，避免无信息的大色块。输入框和键盘出现时允许空态滚动。
- 输入栏文本区与动作区分层；多行输入最多五行，发送/停止位置固定。未配置时提示去配置，不能丢掉草稿或伪造发送成功。
- 用户消息靠右、采用主色浅面；AI 回复偏阅读排版，给予正文更大宽度。模型名、思考、正文与操作有清楚层级，不把全部内容挤进同宽的两种大气泡。
- 思考区以雾紫浅面/细边界标识，头部含图标、状态与折叠箭头，触控高度至少 48dp。流式默认展开、完成默认收起；用户手动展开/收起后尊重选择，状态切换不能重建丢失偏好。
- Markdown、代码复制和错误文案保留。消息用稳定身份；用户停留底部时跟随增量，回看历史时不抢滚动，提供“回到底部”。

### 侧栏

- 宽度约屏宽 88%，不超过 400dp；保留从主页面向右滑打开和 Android 原生边缘返回。
- 结构：品牌头 → 新会话 → 可滚动/可筛选会话 → 底部固定设置。选中行清楚，不为每一行堆玻璃模糊。
- 重命名、置顶、删除仍通过真实仓库动作执行；删除必须确认。设置跳转先关闭侧栏。

### 模型与推理

- 使用可避让键盘的底部面板，标题/关闭、搜索、模型列表、当前选择与推理控制、确认操作清晰分区。
- 模型列表按服务商标识，搜索匹配名称或模型 ID，惰性构建；没有模型与没有服务商分别提供配置引导。
- 在一次面板操作中完成模型与推理等级选择，确认后持久化；取消不改已有设置。关闭推理不等于禁用思考内容展示。
- 当前手输模型不在远端列表中时仍显示，不能因 UI 重排使它失效。只为支持推理的模型显示等级选项。

### 设置、服务商与模型管理

- 设置包含简明品牌信息、外观、服务商和关于；每项使用一致对齐的图标、标题、说明、尾部箭头。
- 服务商列表展示预设身份、协议、默认模型与模型数量，新增入口明确；不把模型列表获取成功写成“聊天连接已验证”。
- 编辑页分服务商/协议与地址、凭证、模型三个区。测试反馈紧邻连接字段，保存固定在底部且有进行中反馈；长列表不影响保存可达性。
- 预设选择支持搜索，协议选择保持可见；编辑旧配置必须保留独立协议与兼容项。Key 默认遮蔽，不进入日志/普通持久化。
- 模型管理支持搜索、手动添加、默认模型、推理开关、可见删除操作；删除确认或清楚的局部撤销，不靠无提示的左滑。拉取结果不抹掉手动模型。
- 添加模型弹窗验证空/重复名称；列表/表单/弹窗的小屏与键盘模式必须可滚动。

### 助手

延续同一套背景、图标与留白。准确显示功能尚未启用，并给返回对话的入口，不在视觉重构中虚构业务功能。

## 7. 弹层、菜单与反馈

- 统一 Dialog：语义图标/标题/简短说明 → 可滚动内容 → 对齐的动作。取消为次级，确认为主色，删除为 `error/onError`。
- 宽度不超过 440dp、水平至少 24dp 外边距，避让键盘和系统区域；按钮无法同行时自动纵向排列，保持至少 8dp 间距。
- 主题选择为有文字标签和选中标记的预览项，三态系统/浅色/深色均真实生效；选项之间留白，不重复套卡片。
- BottomSheet 有拖柄、明确标题和关闭入口；主列表可滚动，确认操作不埋在列表尾。短屏允许整体滚动，不使用超出屏幕的固定高度。
- Menu、SnackBar 与表单验证统一语义色和圆角；不以大块全屏装饰遮蔽内容。

## 8. 动效与 Android 返回

- 路由继续显式返回 Flutter SDK 的 `MaterialPage`，避免 go_router 18 检测到不同 MaterialApp 类型后退化为无动画页面。
- 保持 `PredictiveBackPageTransitionsBuilder` 与 Manifest 的 `enableOnBackInvokedCallback`；普通跳转走系统 FadeForwards，手势返回可预览、取消。
- 新增控件过渡以 180–260ms 为主，颜色/选择/展开自然变化。保留 SDK 的路由时长，不用自制转场替代预测性返回。
- 不添加空页上的无限循环装饰动画，不逐字播放流式文本；仅生成光标可循环，且遵循减少动画设置。
- `PopScope.canPop` 提前确定，禁止 `WillPopScope` 或吞掉系统返回；返回优先关闭当前弹层/侧栏。

## 9. 图标、无障碍与验收

- 所有图标使用 `material_symbols_icons` 的 `Symbols.xxx`，不混用旧 `Icons`。按钮必须有 tooltip 或明确文字/语义标签。
- 触控区域至少 48×48dp；正文/背景对比度至少 4.5:1，最终合成色下也要满足，不把透明度当作降低可读性的理由。
- 验证 320/360dp 窄屏、横屏、键盘、1.3x–2x 文字缩放、长模型名/URL、大量模型、流式思考与完成切换。
- 关键场景用实际 widget 点击、滚动、输入与路由往返测试；渲染浅/深主题检查布局。普通 push/pop 测试不等同于真机预测性返回验收。

## 10. 工程边界

UI 重构不更改 API 协议、推理参数映射、数据表或应用身份。不删除用户配置；装机只做保留数据的覆盖安装。已有功能必须可用，未验证的真实服务调用不能以界面美化为由声称通过。
