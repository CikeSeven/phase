# DESIGN.md — 相月 UI 规范

**月色玻璃，留白优先。** 采用 Material 3 Expressive 的组件形状、操作层级与响应式动效，保留月白/墨蓝画布、克制的彩色表面和局部玻璃质感，服务于 Android 单手操作与长时间阅读。工程边界见 [AGENTS.md](./AGENTS.md)。

### 实现边界

- 使用 Flutter SDK Material 组件，由 `AppTheme`、`AppControlStyle`、`AppSelectionSurface` 与 `AppMotion` 适配当前使用场景；SDK 没有完整的 Expressive 全局开关，不把 `useMaterial3: true` 描述为已启用完整 Expressive 组件库。
- Expressive 不等于更换配色算法：不切换 `DynamicSchemeVariant.expressive`，不引入系统壁纸动态取色；保留原有种子色、明暗语义色、品牌扩展与玻璃材质。
- 按钮状态形状与空间弹簧参考官方 Android Material 的 [Expressive 按钮](https://github.com/material-components/material-components-android/blob/master/lib/java/com/google/android/material/button/res/values/styles.xml) 和 [motion tokens](https://github.com/material-components/material-components-android/blob/master/lib/java/com/google/android/material/motion/res/values/tokens.xml)。当前 SDK 按钮形状仍使用其隐式插值，不宣称与 Android 原生弹簧完全一致；选择面单独使用物理弹簧。

## 1. 设计原则

- **内容先于装饰**：聊天突出正文，设置突出可操作项，表单突出输入与保存；不让品牌卡片、分区标题抢占主任务。
- **层级先用留白，再用色面，最后才用线条**：禁止每项一张描边卡片、框内套框。少线条不等于删除表单边界、焦点提示、错误提示等必要反馈。
- **玻璃是局部材质，不是全局滤镜**：允许无框色面、透明入口和直接铺在画布上的内容共存，不强制所有区域套 `FrostedSurface`。
- **色彩克制而有辨识度**：靛蓝负责主操作，青玉/雾紫辅助功能分组，金色只作小面积月标或置顶点缀；不使用彩虹条、装饰光球或每卡一种渐变。
- **深浅主题独立验收**：使用成对语义色，分别检查合成后的层次和可读性，不机械反相；相同透明度可以使用，但不能因此跳过另一主题检查。
- **功能与文案如实**：只显示已实现的操作与真实状态；不新增口号、教程式提示或没有数据支撑的统计。

## 2. 配色与材质基线

色彩来源为 `lib/core/theme/app_theme.dart` 与 `lib/core/theme/brand_colors.dart`。种子色 `#3D5A98` 经 `ColorScheme.fromSeed()` 生成语义色，不直接当作每个控件的主色。画布浅色为 `#F5F7FC`，深色为 `#0C1423`。

| 色彩角色 | Token | 使用位置 |
| --- | --- | --- |
| 靛蓝 | `primary` / `primaryContainer` | 主操作、选中状态、用户消息、输入焦点 |
| 青玉 | `BrandColors.teal` 及容器配对 | 连接、服务商、AI 标识 |
| 雾紫 | `BrandColors.lavender` 及容器配对 | 思考区、外观设置 |
| 月华金 | `BrandColors.gold` 及容器配对 | 月标、置顶等小面积点缀 |
| 错误 | `error` / `errorContainer` | 失败、删除确认，不借用品牌色 |

- 普通正文用 `onSurface`，次要文字用 `onSurfaceVariant`；彩色容器使用对应 `on…Container`，不把普通正文色硬套到所有底色上。
- 同一页面最多两种主要辅助色面，小面积金色点缀不算第三个主色区；选中和错误还要有文字或图标，不能只靠颜色。
- 业务组件从 `Theme.of(context).colorScheme` / `context.brandColors` 取色；颜色字面量只放主题或代码高亮色表，不维护页面私有调色板。

## 3. 背景、表面与线条

- `AppBackground` 是贯穿页面的月白/墨蓝底与宽幅低饱和色域；背景装饰不接收触控或语义焦点。
- `FrostedSurface` 提供裁剪、底色、可选模糊与细边框；`AppCard` 用于确实需要分组的内容，不是所有行的默认包装。静态卡片和思考区使用 `blur: 0`。
- 当前玻璃填色参考（浅/深）：默认 `0.80/0.88`，侧栏 `0.88/0.94`，Dialog / 长列表面板 `0.96/0.98`。这些是组件基线，不要求无框色面也套用；实际取值以调用点为准。
- 默认模糊强度为 12，必须局部裁剪；不在每条消息、会话或模型行创建 `BackdropFilter`，不靠不断增加模糊和透明度制造层次。
- 边框用于分清输入、浮层或状态，保持轻薄；同一分区不重复叠加外框、行框、图标框和分隔线。不用浓重阴影代替信息层级。
- 装饰图标与信息标签使用无描边色面，前景与底色保持语义配对，不把它们画成可点击按钮。
- 顶栏默认有底部分隔线；设置首页通过 `showAppBarDivider: false` 关闭。局部减线不改变其他页面的全局主题。

## 4. 排版、间距与形状

值来自 `lib/core/theme/app_spacing.dart`、`lib/core/theme/app_radius.dart` 和 `AppTheme`，新增组件沿用 token，不散落相近数值。

| 项目 | 基线 |
| --- | --- |
| 间距 | `xs/s/m/l/xl/xxl/xxxl/section` = 4/8/12/16/24/32/40/48dp |
| 小圆角 | `extraSmall` 8：紧凑控件按压；`small` 12：会话行、选择面按压；`control` 16：中号按钮按压/紧凑按钮选中；`medium` 20：字段、思考区、未选选择面 |
| 大圆角 | `large` 28：通用卡片、输入栏、消息；`extraLarge` 32：侧栏、弹层、设置主面；`full` 40：短胶囊 |
| 页面标题 | `titleLarge` 22sp / 1.25；分区/入口 `titleMedium` 16sp / 1.4 |
| 操作层级 | Filled / Elevated / Outlined 按钮最小高 56dp，标签 16sp；文字与普通图标按钮最小触区 48dp；大字时自然增高 |
| 正文与说明 | `bodyLarge` 16sp / 1.5；`bodyMedium` 14sp / 1.5；元信息 `bodySmall` 12sp / 1.45 |
| 内容限宽 | 设置/表单与底部面板 720dp，聊天阅读区/输入栏 840dp，Dialog 440dp |

- 字体跟随系统，所有字号从 `TextTheme` 取用；不固定整行高度来容纳放大的文字。
- 标题使用强调字重（`headlineMedium` / `headlineSmall` / `titleLarge` 为 700）；聊天正文仍沿用原字号、字重与行高，不把整页文字加粗。
- 操作按钮使用真正随尺寸延展的 `StadiumBorder` 胶囊，而非固定半径冒充全圆角；按压收敛为圆角矩形。卡片、输入框与玻璃浮层仍使用原有圆角档位，不全局改成胶囊。
- 页面水平留白 16–24dp；分区间距 24–40dp，卡片间距 12dp，相关字段间距 16dp；弹层选项至少留 8dp，不把卡片贴在一起。
- 文本起点、图标和尾部操作保持对齐；图标本身通常 20–24dp，可点击触区至少 48×48dp，不能用小图标尺寸替代触区尺寸。
- 长名称采用受约束的换行或省略，保留操作按钮空间；圆角不能吞掉短行的内容宽度。尺寸依父约束，不把整屏宽度用于弹窗内的子项。

## 5. 组件选择与状态

- 共享布局优先复用 `lib/core/widgets/`；仅用于某个业务的外观留在 feature 内，如 `ThemePreview`、消息操作面板，不为一次局部需求修改全局组件默认值。
- 可操作色面使用 Material / InkWell 反馈；无框入口可以透明，但仍应有明确标题、触区和反馈。`ListTileTheme` 不全局加卡片背景。
- 按钮通过 `AppControlStyle` 统一普通、按压、选中与禁用形状；保留各按钮种类和破坏性动作的语义色。提供 56dp 主操作与 48dp 紧凑操作，不为视觉层级改变操作含义。
- 主题、模型和助手选择复用 `AppSelectionSurface`：未选 20dp、选中 28dp、按压 12dp 的形状反馈；色面保留浅主色，选中还需实心勾与读屏语义。弹簧只改变形状，不缩放触区、不移动相邻行；选中标记预留固定宽度。
- 滑杆使用 SDK 的 `HandleThumbShape`、`GappedSliderTrackShape` 与 16dp 轨道；不通过已弃用年份标志切换外观，不改变分段、推理等级或保存时机。
- 图标沿用 `material_symbols_icons` 的现有 `Symbols.xxx`，不混用旧 `Icons`；无后缀图标与 `_rounded` 是不同字形，不能把当前实现写成已统一 Rounded。改变字形须作为独立视觉调整。
- 纯图标按钮必须有 tooltip/语义标签；装饰预览不重复朗读，选择项暴露选中语义，错误提示可由读屏获知。
- 加载、空数据、失败、保存中分别表达；不把加载失败当成“尚未配置”，不留无作用的按钮，不用空白页面代替状态提示。

## 6. 页面与导航

### 转场和 Android 返回

- 路由显式返回 Flutter SDK 的 `MaterialPage`，保留 `PredictiveBackPageTransitionsBuilder` 和 Manifest 的 `enableOnBackInvokedCallback`；不要退回无动画页面或自制普通转场替代预测返回。
- 普通 push/pop 使用当前 SDK 默认 Material 转场；系统返回手势必须支持进度预览、取消与提交，不将普通转场名称写死为业务约定。
- `PopScope.canPop` 提前反映可返回状态，不使用 `WillPopScope` 临时异步拦截；返回优先处理当前弹层/侧栏，根聊天页无覆盖层时才恢复正常系统返回。
- 选择面形状使用 Expressive fast spatial 弹簧（mass 1、stiffness 800、damping ratio 0.6），快速反向时从当前值/速度继续；颜色使用 200ms 无回弹过渡。SDK 按钮形状使用 200ms 隐式过渡，路由继续沿用 SDK 默认时长与预测返回；不得为动画延迟动作或改变保存、取消、选择生效时机。

### 聊天页

- 顶栏保留侧栏按钮、品牌/服务商、模型入口和新会话按钮；设置留在侧栏。模型下拉箭头紧贴模型名，间距 4dp；短名称不把箭头推到最右，长名称省略且保留箭头。
- 输入栏多行最多五行，发送/停止位置稳定；焦点在编辑框时整个外边框用主题主色，失焦恢复默认，不改变尺寸、不画第二层内部边框。判断依据是焦点，不是键盘是否可见。附件入口为回形针图标与来源面板（拍照/相册/文件），草稿以输入框上方的横向附件条展示、可单个移除；模型未标记支持图片时拦截图片入口。用户消息气泡在正文上方展示图片缩略图（点开大图）与文件标签。
- 发送/停止共用 56dp 触区：发送为圆形，运行态为圆角方形，图标与 tooltip 同步变化；停止不是开关选中态，不增加 toggle 语义，不因动画重复派发动作。
- 空态保留简洁引导与真实入口；发送前失败不清草稿，接受发送后再清理已发送内容；侧栏和弹层往返不主动抢焦点或丢草稿。
- 根任务的运行状态独立于当前查看的会话；切换会话仍保留停止入口，并提供“返回运行中的会话”。启动发现中断任务时显示可达的任务入口；读取失败提供重试，不当作没有任务。
- 模型自动重试期间仍属于运行中，发送/停止入口不变；任务条显示重试次数与等待间隔，切换会话不丢失停止能力。失败尝试保留为历史分支，新尝试替换当前显示的失败回答，不把两次正文或思考拼成一次成功回答。
- 文件工具卡片用文件名和实际结果说明成功/失败，不直接显示原始工具 JSON 或“自动重发、读取当前状态”等 AI 内部恢复规则。文件读不到与对话记录未能保存分别表达，不能混用同一条泛化提示。
- 用户消息右对齐、用主色浅面；AI 正文采用完整阅读宽度，模型名、思考、正文和操作分层，不为整段 AI 回复套一张同宽描边气泡。同一次运行的连续助手消息（工具循环每轮落一条）在界面上合并为一个回答区，工具卡片按 Part 顺序留在其中；被用户消息隔开的不同运行与重新生成的新旧回答仍是各自的回答区。
- 思考仅展示响应中的公开文本/摘要，默认展开并限高内部滚动（260dp）；思考区滚到边界后继续拖动由整条消息列表接管，手指停在思考区里也能一路滑到回复之外；思考结束自动收起并显示耗时统计（已思考 X 秒），用户手动展开/收起优先于自动收起。展开/折叠与自动收起都保留阅读锚点，不被自动滚底打断。
- 接近列表底部时跟随流式增量；回看历史时不抢滚动，右下角提供 48dp 圆形向下图标按钮，不显示“回到底部”文字，但保留 tooltip 与读屏标签。按钮使用中性表面，不抢正文与发送操作。Markdown、代码高亮与独立代码复制保持可用。
- 长按用户/AI 消息或点 AI“更多”打开紧凑底部操作面板；复制仅复制正文，空内容禁用。AI“重新生成”在功能实现后提供，创建新回答而非覆盖原消息，不自动重做历史工具动作；未实现的操作不显示。

### 会话侧栏

- 使用原生 `Scaffold.drawer`，宽 `min(屏宽 × 0.88, 400dp)`；主内容区域右滑时连续跟手，支持反向、取消和左滑关闭，不等松手才执行打开。
- 顺序为品牌/新会话 → 搜索与惰性会话列表 → 底部设置。常规会话行约 56dp，更多按钮触区至少 48dp，大字时自然增高。
- **会话菜单与消息操作不同**：会话更多/长按仍用按钮锚定菜单，右侧对齐按钮，空间不足由 SDK 避让；点击菜单外先关菜单，不顺带关侧栏。
- 开关侧栏不恢复编辑框焦点、不误弹键盘；系统返回先关闭展开的侧栏。进入设置只 push 页面、不先关侧栏，返回后仍保持侧栏状态。
- 重命名、置顶、删除调用真实操作；删除确认说明影响，失败保留可恢复状态。

### 模型与推理面板

- 搜索、横向服务商列表、当前模型、推理控制、确认动作清晰分区；模型惰性构建，空模型与未配置服务商分别提示。
- 顶部为横向服务商标签（标注模型数），下方是所有启用模型的单一列表、按服务商名称分组隔开；点击标签滚动定位到对应分组，不改草稿。搜索时列表与标签都只保留命中的服务商与模型。模型行无描边，选中项使用浅主色面与实心勾，未选项不画空心圆。当前/待确认摘要放在面板标题右侧，不另套摘要卡片。
- 面板选择先保留本地草稿，确认后持久化；关闭/返回不保存。列表外手动模型仍可呈现，拉取失败不清空已有模型。
- 推理等级用滑杆选择（最左关、最右最高），所有模型默认支持推理、不按模型收窄；确定不支持的模型可在添加时关闭以隐藏滑杆。关闭推理不代表隐藏接口实际返回的思考。界面不得擅自改写协议映射。

### 设置与主题

- 首页用一块无描边雾紫外观面作为主入口，透明的服务商配置行作为次级入口；小月标、产品名、开发预览状态与版本作居中静态落款。
- 不恢复品牌卡、“外观/服务商/关于”重复分区或每项一张描边卡；当前页边距 24dp，外观面圆角 32dp，服务商入口前间距 24dp，落款前 40dp。
- 外观主面显示“主题模式”、当前值和 72dp 高的简洁聊天预览；预览用真实 AppTheme 色组，系统态展示明暗两半，不套装饰手机框。
- 主题弹窗选项无描边；选中项用浅主色面与实心勾，未选项不画空心圆。选择只是草稿，应用后切换并保存，取消/返回不改已保存选择。

### 服务商与助手

- 服务商列表显示真实名称、协议与模型数量；编辑页分连接、凭证、模型区，保存固定且可达，测试反馈靠近相关字段。
- 服务商搜索优先，真实数量用次要文字呈现，不单设统计卡；列表采用无框入口，连接与凭证字段直接排布，保留字段本身的边界、焦点与错误提示。
- 保留模型获取、手动添加、勾选启用（仅启用的模型进入聊天选择列表）、能力（推理/工具/图片）编辑及删除确认；字段错误、失败重试和凭据留空语义不能因精简文案而丢失。版本与功能状态以工程事实为准。
- 助手提供真实列表与编辑入口：系统提示词、默认模型、工具范围分组呈现；聊天顶栏首行是当前助手（点开切换），第二行是模型。工具尚未落地时只说明真实状态，不放可点的假开关，也不用占位数据虚构助手或用量统计。
- 已落地工具按文件、网络和系统信息分组编辑“直接执行 / 需要确认 / 禁止使用”；选择只是助手表单草稿，保存后生效，返回取消不改持久化策略。空范围表示全部禁止，不是开放全部默认工具。
- 应用列表、打开应用、控件/截图观察、点击、滚动、输入和手势组合统一为一个“应用操作”策略，不显示各 UI 工具的单独权限开关。

### 应用名单

- 执行设置不再要求选择固定目标 App；默认黑名单模式允许第三方应用，系统应用默认禁止。相月自身与其他第三方应用一同显示、筛选和选择，适用相同黑白名单与工具策略，不另设只读或禁止操作的例外。
- 黑名单勾选表示禁止；系统应用默认勾选，取消勾选可以逐个放行。白名单仅允许勾选项；两种模式的名单独立保留，不自动互相取反或覆盖。
- 名单面板可搜索名称/包名，筛选全部/第三方/系统应用；支持名称、首次安装时间（新到旧）、安装包大小（大到小）排序，未知大小排在最后。大小只统计 APK 与 split APK，不包含私有数据或缓存，文案必须明确。
- 列表惰性构建，筛选/排序不改变选择；面板确认仅更新页面草稿，页面保存后生效，取消/返回不保存。原生名单同步失败必须明确反馈并停止正在运行的任务，允许重试。
- 名单收紧立即限制运行中的应用操作；新允许的应用从下一次任务开始生效。AI 应用列表和每次实际操作使用同一有效名单，不只在界面隐藏条目。

### 中断任务

- 中断任务页展示原会话、已存工具状态与继续/停止入口；已保存结果不自动重做。未决确认沿用原期限，过期后按拒绝显示。
- 中断调用按失败收口，继续时自动把已有响应和错误回填给 AI；没有结果核验入口、核验状态表单或因此禁用的继续按钮。AI 可以重新读取界面/文件判断后续步骤，不自动重发旧动作。

### 执行中的确认与控制

- 待确认请求由应用级状态持有，聊天或设置路由只负责展示；关闭面板不批准、不重置期限，聊天任务条提供“查看待确认动作”重新打开同一请求。
- 相月在前台时由 Flutter 显示确认；离开前台只移交展示权，返回后仍使用原调用、参数和截止时间。迟到、重复或其他调用的决定不生效。
- 设备工具任务期间显示系统任务通知，提供返回相月与停止入口；通知不显示对话、工具参数或执行内容。纯聊天、私有文件与 HTTP 工具不因此常驻设备任务服务。
- 原生执行条与确认面板使用任务范围内的 Accessibility overlay，不做常驻悬浮球；其具体交互随后续无障碍实现验收，不能用本批桥接/通知测试代替。
- 执行设置如实显示窗口截图能力（Android 14+、已连接无障碍）及模型需支持图片/工具的条件；不把权限开关显示成模型视觉已验证。
- 截图工具卡片显示实际图片尺寸，产物入口可放大查看。手势组合确认展示目标应用、坐标空间（默认屏幕像素；图片坐标附宽高）和完整有序参数，不再要求截图 ID；拒绝整组不派发。结果摘要展示系统已完成步数和具体原因，不直接展示原始截图 JSON，不把系统手势回调描述为任务目标已完成。手势完成但后续截图不可用时明确区分，不把动作改标为失败。
- 截图观察无参数，确认文案为读取当前前台页面，不要求填写应用包名；实际目标由原生识别并受应用名单约束。手势不以前置截图、截图有效期或一次性使用为条件，目标和坐标由模型提交，不要求用户补填。

## 7. 弹层、键盘与文案

| 场景 | 组件与布局 |
| --- | --- |
| 重命名、删除确认、主题选择 | `AppDialog`；标题必需，图标/说明按需，普通内容滚动；懒列表须 `scrollableContent: true` |
| 模型/预设搜索等长列表 | `AppSheet`；扣除键盘等父约束后占可用高度 85%，正常高度下 footer 固定，短窗口整体滚动 |
| 消息复制等短操作 | 原生模态 BottomSheet + `MessageActionsSheet`；按内容高度，不用 85% 高度的大面板 |
| 工具确认 | `AppSheet`；标题为工具名，副标题是动作摘要，正文是执行通道、目标与逐条真实参数（长正文限高内滚动），标题右侧显示剩余秒数；footer 三个动作自上而下各占一整行：停止任务（错误色）/ 拒绝 / 允许一次。到点未决定按拒绝，关闭等同于未决定 |
| 导出会话 | 原生模态 BottomSheet + 两个 `ListTile`（Markdown / JSON），选中后导出并提示文件路径 |
| 查看产物 | 文本 `AppSheet`（限 256KB 预览），图片 `AppDialog` + `InteractiveViewer`；文件丢失或二进制内容明确说明，不显示空内容 |
| 会话更多操作 | 按钮锚定 `MenuAnchor`，不是消息的长按菜单 |

- BottomSheet 保留拖柄、标题、关闭入口；未提交时支持返回、点遮罩和下滑关闭，取消不执行动作，保存中遵循防重复与离开保护。`AppSheet` 的 child 默认是有界可滚动区域，静态内容设 `scrollableChild: false`，不要双重无界滚动。
- `AppSheet` 常规 footer 最多占面板高度 32%；工具确认的三个 56dp 动作显式使用 `footerMaxHeightFactor: 0.5`，保证正常视口全部可达，不改变其他面板默认布局。极短窗口仍由整个面板滚动。
- 键盘避让只做一次，保留底部安全区；短屏或横屏允许内容与操作滚动到达，不能靠固定高度截断按钮。弹层不主动聚焦聊天输入框。
- Dialog 操作区取消在次、确认在主；破坏性动作使用 error 语义色并说明后果。重复提交锁定、保存失败可重试。
- 保留字段含义、当前值、错误原因、删除影响、版本与必要凭据说明；删掉“支持多行输入”、重复口号、复述按钮用途的长提示，连同多余留白一起删除。
- 成功/失败反馈使用简洁 SnackBar 或就地提示，不暴露异常堆栈、鉴权信息或原始响应。

## 8. 无障碍与性能

- 正文与背景的最终对比度至少 4.5:1；不能只检查透明色的原始值，也不能用降低文字透明度制造“高级感”。深浅主题和放大字号均需检查。
- `disableAnimations` / `accessibleNavigation` 时，玻璃组件去模糊并合成实底；`AppMotionTheme` 将 SDK 按钮形状过渡置零，选择面停止弹簧并立即到达当前状态，模型标签定位不动画滚动。生成光标可见但静止，离屏停止动画。不在空态添加无限装饰动画，不逐字播放流式文本。
- 消息/会话/模型列表保留惰性构建与稳定身份；不要让展开/收起、键盘变化或弹层往返重置阅读状态。
- 玻璃和拖动性能在目标 Android 的 Profile 构建上检查；记录设备刷新率、UI/raster 帧耗时和超时帧，比较同模式、同场景。截图成功、Debug 观感或 widget 运行时间不能证明流畅。
- 不在没有帧数据时把卡顿归咎设备或某段 `setState`；`RepaintBoundary` 也不能代替对实时背景模糊成本的测量。

## 9. 验收入口

按本次改动选择相关回归，代码交付仍执行 AGENTS 的标准检查；以下是验收要求，不是已经覆盖全部场景的声明。

- **布局**：320/360dp、横屏、1.3x/2x 字号、键盘、长名称、长消息与大量列表；检查触区、截断、按钮可达性及浅深主题。
- **Expressive**：`test/core/theme/app_theme_test.dart` 验证品牌色与正文基线不变；`test/core/theme/expressive_controls_test.dart` 验证真实按压/释放、禁用、键盘、快速改选、减少动画、大字和滑杆交互。
- **聊天/侧栏**：`test/features/chat/chat_page_layout_test.dart`、`test/features/chat/chat_transcript_test.dart`、`test/features/chat/message_bubble_test.dart` 验证图标位置、焦点、跟手、阅读与复制。
- **设置/弹层**：`test/features/settings/settings_page_test.dart`、`test/core/theme/app_panels_test.dart` 覆盖主题确认/取消、持久化和滚动约束。
- **路由**：`test/route_transition_test.dart` 与 `test/ui/predictive_back_test.dart`；普通 push/pop 测试不替代系统手势通道或真机返回预览检查。
- **渲染预览**：`test/ui/ui_preview_test.dart` 默认跳过，可生成 390dp 常规字号与 320dp 2x 字号的浅深聊天、侧栏、设置、主题弹窗、模型选择和服务商页面到 `build/ui-preview/`，包含回看历史时的纯图标返回底部按钮；目前不自动截图消息操作面板。

预览用测试数据和临时中文字体，不读取手机配置、不把测试字体加入应用资源；字体已存在时可跳过下载：

```bash
mkdir -p build/ui-preview
curl --fail --location --output build/ui-preview/NotoSansCJKsc-Regular.otf https://raw.githubusercontent.com/notofonts/noto-cjk/main/Sans/OTF/SimplifiedChinese/NotoSansCJKsc-Regular.otf
flutter test --dart-define=CAPTURE_UI=true test/ui/ui_preview_test.dart
```

交付区分：布局/交互测试通过、截图生成成功、实际查看截图、真机操作与性能测量。没有查看图片或操作设备时，不声称已完成视觉或真机验收。
