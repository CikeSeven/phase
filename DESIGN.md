# DESIGN.md — 相月 UI 规范

**月色玻璃，留白优先。** 采用 Material 3 Expressive，保留月白/墨蓝画布、克制的彩色表面与局部玻璃质感，面向 Android 单手操作和长时间阅读。

本文规定视觉、交互与验收要求，不表示所有场景已经通过验收。产品现状与契约见[产品设计](./docs/product_and_technical_design.md)，新增功能见 [Agent 与扩展设计](./docs/agent_extensions_design.md)，工程流程见 [AGENTS.md](./AGENTS.md)。

## 1. 设计原则

- **主任务优先**：聊天突出阅读与输入，设置突出选择与操作；装饰、品牌和统计保持次要。
- **层级克制**：先用留白，再用色面，最后用线条；同类控件统一反馈，避免框内套框、逐项描边和满屏胶囊。
- **状态真实**：区分加载、空数据、成功、失败、保存中和禁用，不显示无效入口或无依据的统计。
- **操作可预期**：选择、取消、返回和保存语义一致，视觉调整不改变用户选择与业务结果。

## 2. 配色基线

品牌靛蓝基准为 `#3D5A98`；浅色画布为 `#F5F7FC`，深色画布为 `#0C1423`。

| 色彩角色 | 用途 |
| --- | --- |
| 靛蓝 | 主操作、选中状态、输入焦点 |
| 青玉 | 思考、连接、服务商与 AI 辅助标识 |
| 雾紫 | 工具调用、外观与辅助分组 |
| 月华金 | 少量月标、置顶等点缀 |
| 错误色 | 失败、删除与危险操作 |

- 使用共享语义色，前景与背景成对设计；同页最多两种主要辅助色面，小面积金色点缀不计入。
- 深浅主题分别检查最终合成效果，不机械反相或照搬透明度；对比度要求见第 8 节。

## 3. 背景、表面与线条

- 画布采用宽幅、低饱和色域，不用装饰光球或逐卡渐变；背景不承载操作与状态。
- 玻璃集中在顶栏、悬浮输入栏和必要浮层，模糊限于自身边界；长列表与正文不重复模糊。顶栏用低遮盖半透明底色、局部磨砂和克制的斜向高光，弱化底部分隔线，保持原有高度与触区；输入栏可用轻高光和阴影。输入栏的权限菜单与上下文用量面板使用局部磨砂玻璃，底色不透明度浅色约 62%、深色约 70%，保留 20dp 模糊和清晰文字；模式条目的色面保持半透明，不遮成整块实底。其余菜单、弹窗与面板保持较实底色。
- 卡片只分组相关内容，普通列表可直接铺在画布上；外框、行框、分隔线和阴影不重复强调同一边界。

## 4. 排版、间距与形状

| 项目 | 基线 |
| --- | --- |
| 间距 | 4dp 网格；常用 4 / 8 / 12 / 16 / 24 / 32 / 40 / 48dp |
| 页面留白 | 水平 16–24dp；分区 24–40dp；相关字段 16dp；相邻卡片 8–12dp |
| 圆角 | 8 / 12 / 16dp 用于紧凑控件及按压态；20dp 用于字段和选择面；28 / 32dp 用于较大表面 |
| 标题 | 页面 22sp / 1.25 行高；分区 16sp / 1.4 行高 |
| 正文 | 16sp / 1.5 行高；说明 14sp / 1.5；元信息 12sp / 1.45 |
| 触区 | 主操作至少高 56dp；其他操作至少 48×48dp；视觉色面可小于触区，图标通常 20–24dp |
| 内容限宽 | 设置、表单和底部面板 720dp；聊天阅读与输入 840dp；对话框 440dp |

- 字体跟随系统缩放；长名称可换行或省略，关键状态、操作和字段标签必须可读。空间不足时换行、重排或滚动，不缩小文字与触区。
- 文本起点、图标和尾部操作对齐；布局按所在容器宽度计算。主按钮用胶囊轮廓，字段、卡片和浮层保留形状层级。

## 5. 控件、反馈与状态

- 主次操作用填充、色面和留白区分；删除、移除、卸载和替换已有环境等危险操作的入口、弹窗图标与确认按钮统一使用错误色，填充确认按钮使用 `error/onError`，图标色面使用 `errorContainer/onErrorContainer`。取消与普通操作保留原语义色，禁用态保持弱化；点击有按压与焦点反馈，装饰不伪装成按钮。
- 按钮、列表入口与可点击卡片使用可反向的按压形变，触区和相邻布局保持稳定；标准 `SegmentedButton` 沿用 SDK 的按压与选中反馈，不自绘滑动底板；静态内容不做按压动效。
- 选择面、下拉选项和多选名单共用选中语义：浅主色面、实心勾与预留标记空间；未选 / 选中 / 按压圆角以 20 / 28 / 12dp 为基准。标准 `SegmentedButton` 仅选中项显示勾，未选中标签居中、不预留图标空位。
- 短选项用锚定下拉，短操作用条目菜单或按内容展开的面板，长列表用可搜索面板。锚定菜单圆角 28dp，操作项按压从 20dp 收敛至 12dp，不显示选择勾；避让屏幕边缘，禁用时不可开启或提交。
- 动效短促、可中断、可反向，不延迟操作；形状可有轻微弹性，颜色以约 200ms 无回弹过渡。锚定菜单展开约 180ms、收起约 120ms，整块以 0.96 → 1 的轻缩放与淡入呈现，展开先快后缓，不做条目错峰淡入或逐帧高度重排；玻璃菜单独立渐变底色、边缘、模糊强度和内容，不在背景模糊外包整体透明度层，避免开合时底色跳变。快速反向沿用当前进度，收起即停止条目交互，不残留遮罩或返回层级。减少动画时直接切换。
- 未知进度使用 Expressive 形变指示，按钮内沿用前景色；只有真实可计量的进度使用进度条。局部加载或失败不遮挡无关操作，错误就近说明并提供恢复入口。

## 6. 页面与导航

- 遵循 Android 导航习惯，预测返回支持手势进度、取消与提交，侧栏拖动跟手且可反向。聊天普通正文区域保留右滑打开侧栏，不依赖屏幕边缘；侧栏打开的横向起手门槛至少 36dp 且不低于系统触摸阈值的两倍，正文滚动、输入编辑和局部横滚优先参与手势竞争，避免短距离偏移误开。该门槛不改变正文、顶栏、输入框和侧栏内容的触控阈值，也不提高已打开侧栏的关闭门槛；代码、表格和块公式的横向阅读区优先滚动内容，不被侧栏打开手势抢占。
- 返回先关闭最上层菜单、弹层或侧栏；点击遮罩只关闭当前层。往返保留滚动位置、搜索与草稿，不抢焦点或主动弹出键盘。

页面交互要求：

- **聊天阅读**：用户消息用色面与 AI 正文区分；用户消息下方与助手标题上方各留 4dp，合计 8dp；助手回答到下一条用户消息保留 24dp 分组留白，不压缩标题操作触区。正文、思考和工具按实际顺序呈现。长正文与代码可阅读、复制；接近底部时跟随新内容，回看时不抢滚动。
- **回到底部**：使用 56×48dp、16dp 圆角的矩形玻璃图标按钮，局部模糊 20dp，底色不透明度浅色约 38%、深色约 54%，配轻高光与细边缘，不使用圆形实底。按列表方向判断：离末端超过 96dp、向最新消息滚动时显示，回看历史、到达底部或点击后隐藏；停手保留当前显隐，流式输出和内容展开不主动唤出。入场约 260ms，以淡入、轻上移和小幅弹性缩放呈现；退场约 180ms，向下收拢淡出，立即退出触控与读屏，快速反向沿用当前进度。点击时按钮顺势下沉，列表按距离以约 420–820ms 的强调缓动先加速、再柔和减速；惰性布局修正的尾端继续短缓停，不在动画末尾大幅跳转。触摸或手动滚动可立即接管，切换会话不沿用旧动画，到底后恢复流式跟随；减少动画时直接显隐和定位，关闭玻璃模糊仍按第 8 节执行。
- **助手 Markdown**：正文直接铺在画布上，保留左右 16dp 留白，使用 16sp / 1.6 行高；段间以约 12dp 为视觉目标，流式分块和字体行盒不保证严格像素间距。H1–H6 为 24 / 22 / 20 / 18 / 16 / 16sp，标题自身前 16dp、后 8dp，不自动附加分隔线；列表标记与正文间 8dp、条目下方 4dp。正文中性，主要标题与链接用靛蓝，引用淡色底、左侧标记和 12dp 内边距，行内代码用淡主色背景、无边框、左右约 4dp 留白，且可随正文换行。代码与表格 15sp / 1.5 行高、16dp 圆角；代码内容四周 16dp，表格单元格水平 12dp / 垂直 8dp，表头淡色、行分隔克制。代码、整表和块公式分别横滚，溢出时直接拖动内容，不显示滚动条，不缩字、截断或强制单元格软换行；代码语言与复制按钮固定在头部。代码高亮使用本地常用语法及其别名，以深浅主题专用的高对比语法色区分关键字、字符串、数字、类型、函数与属性，关键字与函数适度加重；未知语言单色显示，流式未闭合代码先完整显示原文、闭合后着色。复制成功后按钮以约 200ms 淡入淡出与轻缩放切换为勾图标，2 秒后恢复，再次复制成功重新计时；不弹底部成功提示，失败保留错误提示，代码变化清除旧成功状态，减少动画时直接切换。不增加正文玻璃、阴影或整条回答外框。
- **HTML 代码预览**：语言标记为 `html`（忽略大小写和首尾空白）时，在代码卡片复制按钮左侧显示预览图标；其他语言不显示。点击打开独立页面，使用 WebView 展示点击时的原始 HTML，支持 CSS/JavaScript，流式更新不自动刷新已打开的预览。页面保留返回、加载和失败重试，不改写聊天内容；返回仍停留在原阅读位置，沿用 Material 页面转场与预测返回。网页内容不强制套用应用主题。
- **顶栏与输入**：助手、模型入口分开，长名称给操作留位。聊天内容可滚到顶栏后呈现真实磨砂，首条消息按顶栏及运行提示实测高度留白，运行提示固定在顶栏下方。输入栏悬浮于阅读区，文本与附件/发送操作分层，容器底部与文本区底部内容内边距均为 4dp，不压缩输入与操作触区，消息末尾留足避让空间；发送与停止共用稳定位置，发送未接受时保留草稿。切换会话仍可查看运行状态、停止任务并回到原会话。
- **主页模型选择**：分组标签与模型条目使用紧凑留白，保留至少 48dp 触区。模型选中后自动保存，推理滑杆松手后自动保存最终等级，面板保持打开；不设底部取消与确认操作行。写入按顺序执行，不阻断继续选择或关闭，不切换整面板的禁用外观与布局；过期完成不覆盖较新选择，失败就近提示并可重试，关闭后的失败用消息提示。
- **折叠内容**：思考与工具分别使用青玉、雾紫的不透明容器底色，标题与图标使用对应前景色，收起和展开时都与正文保持清晰分层，不叠加外框与阴影；两类卡片统一仅在下方留 4dp 外间距，相邻卡片间隔为 4dp，不随思考与工具的先后顺序变化；标题行水平内边距统一为 12dp，左侧图标、文字起点和展开箭头对齐。默认收起，整行切换且有展开语义；更新与滚动回看保留手动选择，重新进入会话恢复收起。展开、收起保留标题位置；长内容在限高区域滚动，边界处继续拖动可滚动消息列表。
- **思考**：只展示实际公开内容；耗时按各段接收阶段计量，正文或工具输出开始后停止，无计时依据不推算。接收期间计时标题每 100ms 独立刷新，不依赖文本增量，减少动画不暂停计时；进行中和结束后的秒数统一保留一位小数。
- **工具调用**：按工具语义展示，不使用“输入参数 / 输出内容”表单分区。卡片与思考区同宽，不重复添加水平留白；diff 使用明显的红绿底色及配套前景色，与工具卡片底色清楚区分，中性状态文字保持足够对比度。调用文字用等宽字体与适度加重，返回内容用常规正文体。收起显示工具名、命令或文件路径、状态和箭头；展开后命令显示 `$ 命令` 与终端输出，仅在非默认时补充目录、超时，仅在异常时显示退出码等信息。文件列表只显示逐行文件名或相对路径，目录带 `/`，不展示参数与结果 JSON；空目录、分页和实际错误保留。文件写入内容用绿底 `+`，编辑用统一 diff（删除红底 `-`、新增绿底 `+`、上下文中性），不虚构绝对行号或覆盖前的旧内容。成功不重复堆叠回执，失败、取消和实际警告保留。未知工具保留格式化调用和完整返回，不猜测第三方字段。展开后的调用、返回与产物共用一个限高滚动区并保留位置，标题留在滚动区外；调用与返回同时存在时，仅用一条细圆角分割线隔开，不各自滚动。长内容可选择、复制；分页、截断、附件与存储故障明确呈现。普通命令不附 stdout/stderr 文件，超出预览上限才提供完整日志，用户生成的文件仍作为产物展示。
- **应用工具卡片**：标题使用实际应用名，不铺开包名、快照 ID、窗口或权限字段；名称无法读取时明确显示不可用，不猜名称、不弹权限请求。点击、滚动、输入仅展示控件 ID、方向或输入文字；截图观察显示应用名及可点开的截图；手势显示“执行 N 步手势”、应用名、坐标空间、坐标和必要时长。列表/界面观察整理为应用名称或控件内容；`inspect_ui` 标为“读取界面控件”，无控件时说明此次读取不包含截图，避免与“截图观察”混淆。保留实际失败原因、界面未变化、手势已完成和已派发信息；只改变阅读呈现，原始记录和模型回填保持不变。
- **附件**：拍照、相册、文件使用紧凑来源面板，大字号改为纵向条目。工具附件文件名用 12sp，图片预览文件名用 14sp；卡片内最多两行省略，打开后可读完整名称。
- **侧栏与表单**：侧栏突出新建、搜索、会话和设置，更多菜单锚定条目。需确认的设置、服务商新增和助手编辑突出当前值与保存入口；确认前只改草稿，取消不保存，保存中防重复提交，失败保留草稿。服务商新增与助手编辑的保存、助手删除使用顶栏右侧图标按钮，不设底部操作栏；内置默认助手不显示删除按钮，不额外显示说明。切换无关字段不覆盖选择。
- **模型目录**：服务商页显示内置快照或最近拉取时间；空配置页也可手动刷新，刷新中防重复，失败保留原目录。模型目录按模型 ID 跨服务商匹配，自定义 API 和 Ollama 也参与；精确 ID 优先于命名空间末段，同级优先已选预设，再按模型 ID、服务商 ID 排序。模型参数只显示字段、参考数值与校验错误，不展示匹配规则或预算公式；目录值只作提示与预算校验，不回写手填配置，不改变协议请求。
- **服务商编辑**：已有配置的文本输入停顿后自动保存，协议、预设与模型修改后自动保存，不设手动保存和底部操作栏；新增仍确认后创建。写入按顺序执行，保存不锁定编辑、不关闭页面、不抢焦点，返回时继续保存有效修改；无效输入不覆盖配置，失败保留输入并提供重试，离开后的失败用消息提示。删除位于顶栏右侧，确认后清除配置、模型与对应凭据；等待已派发写入结束再删除，部分失败可重试，不能被迟到保存重新创建。
- **选择与名单**：搜索、筛选、排序不改变勾选。黑白名单说明勾选含义、数量、排序方向与大小口径；应用类型与排序并排，大字换行。未授权或仅返回本应用时提示访问受限，提供授权和重试，禁用确认；从权限设置返回后刷新且保留草稿。
- **执行与权限**：页面只放任务通知、无障碍服务和应用名单，不放截图说明或文件授权区。区分已授权、未授权、读取中与失败，授权不等于能力已验证；仅申请系统提供的应用列表运行时权限，拒绝后不因前后台切换重复弹窗。结果说明实际状态，不要求用户补填系统可观察的信息。
- **任务悬浮窗**：使用可拖动、左右吸附的侧边卡片。收起约 168dp 宽，模型输出正文时直接显示最新正文原文，按实际宽度与字号保留末尾两行、随流式更新原位替换，不用“正在回复”等状态文案占据正文位置，也不逐 token 播放切换动画；其他阶段显示状态，始终保留一个关键操作；等待用户操作时显示继续，展开后停止仍可达。展开约 264dp，直接显示按实际顺序更新的最新消息列表，不设动态/思考/正文分页；列表限高约 136dp，最多保留最近 6 项、每项最近 480 字片段，完整内容回会话查看。近底部跟随更新，回看时不抢滚动；工具完成后到下次模型输出前保留“执行了 xxx”或实际失败/拒绝状态。展开收起、终态与吸附以约 220ms 可中断的尺寸/透明度动画过渡，状态文字短淡入滑动，新消息轻入场，不逐 token 闪烁；系统关闭动画时直接切换。截图、锁屏与前台临时隐藏立即收尾动画，不改变确认、继续或停止的时机。确认参数在列表内限高滚动，允许、拒绝、停止保持可达；等待操作不用确认倒计时。结束后只留返回箭头与关闭图标，均有无障碍标签；点击返回先销毁本次面板再打开相月，再切回原 App 不重现。展开收起与临时隐藏保留吸附位置，完整记录只在会话持久化。

新增功能接入时遵守以下规则；功能可用前不显示占位入口：

- **扩展管理**：MCP、Skills 和插件在独立扩展页面管理，展示来源、启用范围与实际连接/安装状态。连接成功不标成工具执行成功，安装成功不自动启用到助手；更新展示实际能力变化。
- **会话工作区**：空白聊天页仅保留草稿；发送第一条消息时自动创建会话及独立工作区，同轮为 AI 注入可用的命令工具。会话建立后顶栏文件入口直接打开本会话工作区根目录；文件工具使用相对于该根目录的路径，产物不要求放入 `output/` 子目录。环境设置不展示会话工作区列表；文件从所属会话进入，不提供手动创建、选择、解绑或单独删除工作区。删除会话确认明确包含消息、附件、工作区与产物；活动任务先停止，清理失败保留重试入口。复制会话保留独立文件副本。
- **环境设置**：与无障碍权限页分开。Ubuntu 图标、版本与操作合并为紧凑暖金色卡片，不展示“可用”、静态下载量及安装大小；未安装时右侧使用下载图标加“安装”的紧凑主按钮；已安装时横向排列无底色、无描边的重装与卸载图标按钮，使用错误色并提供长按提示与读屏名称。操作保留至少 48dp 触区，窄屏或大字时整组移至下一行并保持右对齐。环境依赖标题不附说明，开发依赖使用雾紫色卡片，描述仅为“Python、Node.js、Git与ripgrep”；已安装详情仍从卡片进入。下载期间只展示实时大小。读取与安装进度固定在应用栏下方，使用 Expressive 横向波浪进度条，背景与下方页面画布连续，不随正文滚动；下载按实际字节更新，并显示百分比和已下载/总大小，校验、解压、检查等无总量阶段使用不定进度，不推算完成比例。减少动画时使用静态线条，真实进度继续更新。缺少环境只影响需要它的工具，普通聊天可继续。清理、重置、卸载说明环境、依赖和用户文件各自的影响。
- **安装阶段**：基础环境与开发依赖分别显示步骤序号、完整阶段顺序、当前操作说明和本步骤用时。软件源更新明确是下载包索引，不是安装依赖；APT 输出自身的传输量、百分比、速度与等待状态，不换算成整体安装百分比。最近输出保留最新行并换行展示，失败/取消保留停止步骤与输出；静默时显示距上次更新的时长，不把无输出直接断言为网络故障。环境依赖安装期间禁用环境替换与卸载。
- **会话主环境**：只在环境设置显示“新会话主环境”的 Ubuntu / Termux 全局单选，默认 Ubuntu。按用户指定的 [Material 3 Segmented buttons](https://m3.material.io/components/segmented-buttons/overview) 使用 Flutter SDK `SegmentedButton`，不替换为 Connected button group 或自绘滑块。保留整体圆角描边和分段线，选中项用浅色 `primaryContainer/onPrimaryContainer` 配对与勾图标，标签使用 500 字重，未选中标签居中且无图标占位；按实测字宽固定紧凑组宽，不铺满页面，保留至少 48dp 触区，大字空间不足时纵排。未安装、未获系统命令授权或尚未取得状态时只禁用 Termux 选项并弱化文字，不连带锁住 Ubuntu；保存前重新核对授权，不用旧状态放行。Termux 不设额外使用开关，执行仍需初始化与外部调用配置就绪。按压、选中及读屏语义沿用 SDK，减少动画时直接切换。选择即保存，临时防重复锁不切换整组置灰外观，写入成功后更新选中；超过 200ms 才在标题旁固定槽位显示加载，不触发顶栏进度或页面位移，失败就近提示并保留原选择。新会话首次发送时绑定已保存值，之后不可切换；修改全局设置不改变已有会话，切回 Ubuntu 不停用已有 Termux 会话。聊天输入框不增加环境入口或草稿。文件页标题展示已绑定环境，命令、文件和 Skill 使用同一后端；两边文件独立保留，文件菜单提供“复制到另一环境”，确认显示路径与同名覆盖、目录合并影响，不增加双栏或自动同步。
- **外部命令通道**：环境设置以两张紧凑中性色面卡片展示 Shizuku、Termux，卡片间隔 12dp、水平内边距 16dp、垂直内边距 12dp；图标、上下排列的名称与实际状态、右侧展开指示在同一内容行，不保留底部操作区、三点图标或“设置”文字按钮。点击卡片展开当前可用操作，按压反馈沿用共享交互表面；右侧指示与局部加载复用同一固定槽位。只有 Shizuku 在行内保留独立开关，开关点击不触发卡片操作；默认关闭，服务未运行、未授权或身份不受支持时禁用开启，保存前向原生复核，失败保持原值，已开启后失去授权仍可关闭。大字时仅把 Shizuku 开关移至下一行，状态自然换行，不截断；Termux 不显示使用开关。下载、打开应用、授权、连接/初始化、Termux 外部调用设置复制均从卡片菜单进入，按实际状态开放，状态未知时保留重试，不支持的设备不展示无效操作。菜单支持返回关闭，状态变化或操作锁生效时关闭，不能提交过期动作。授权不标为连接验证成功，授权与初始化不自动改变主环境或 Shizuku 开关。操作不触发页面顶栏进度，超过 200ms 才在对应卡片右侧的固定槽位显示加载；临时操作锁保留控件颜色，但禁用重复操作及对应读屏动作。不设底部刷新按钮，沿用返回前台和通道事件更新。Termux 依赖自行管理，Ubuntu 依赖卡片不改为全环境安装。命令确认显示真实通道、身份、目录与完整命令，传输显示两端路径和方向；目录合并不删除额外文件，失败保留已完成项，无终止回执不显示“进程已停止”。删除会话包含两侧托管目录，不影响任意外部导出文件。
- **命令与终端**：确认显示环境、工作区、完整命令及超时，结果区分退出码、停止、超时和输出上限。终端页面返回保留所属会话，停止操作明确作用对象；长输出可滚动/打开产物，截断必须标明。
- **权限模式**：输入栏显示本会话的计划/基础/全权限模式，入口在可用区域靠右排列，与上下文圆环之间保留 8dp 间距，保持原有触区；计划用青绿色、基础用蓝色、全权限用雾紫色，入口与菜单条目配色一致，深浅主题均保留选中标记。点击向上展开无阴影的磨砂玻璃小菜单，只显示模式名称与选中标记，不附说明段落；菜单约 203dp 宽，相邻条目背景之间保留 8dp 间距，条目保留至少 48dp 触区。选择即关闭并保存，不设确认面板。会话独立记忆，新会话默认基础；空白页仅改草稿、不落库。模式写入成功才生效，保存期间禁用重复切换、发送和重新生成，失败保留原档与输入。运行中不可切换，换会话关闭菜单且迟到保存不改另一会话。菜单避让键盘与边缘，返回只关闭菜单。助手只选扩展和记忆范围，不提供执行策略。
- **上下文与记忆**：批准计划后恢复规划前的执行档，并展示实际模式。计划在聊天中的计划卡片编辑与批准，不另设“计划与上下文”页面；摘要生成完成不等同于当前分支可复用。记忆在独立设置页可检索、编辑、停用和删除，显示助手/全局范围与来源；助手中的记忆范围只在保存后生效，写入遵循会话模式。
- **用量统计**：输入框下方、权限模式右侧常驻上下文圆环；视觉直径约 26.7dp、线宽 4.5dp、中心百分比约 7.3sp，均保留系统字号适配与至少 48dp 触区。新会话未发送时显示 0%，不因此创建会话或统计记录；已有占用不足 1% 显示 `<1%`，不误显为零。发送准备期间保留上次占用；运行中在每次模型请求前、请求收口落库后和每个工具结果回填后更新，不冻结整轮，也不等下一请求才显示新增内容，结束后再采用空闲预览。上下文仍是有效 usage 校准后的估算，无有效基准时使用本地估算；显示刷新不触发摘要或额外 API 请求。新计量未就绪时不清零，圆环和数字以约 360ms 平滑更新；减少动画时直接更新，不逐 Token 重算或动画。换会话不借用另一会话的数据。点击向上弹出约 240dp 宽的紧凑小面板，围绕圆环定位并向右避让屏幕边缘，只保留已占用上下文、总上下文、缓存命中率三项，不附来源、口径和覆盖范围说明；估算带约号，缓存未知与零分母分别显示未提供 / 不适用。面板避让键盘、大字短屏可滚动，外部点击或返回关闭且保留输入焦点与草稿。缓存不减少上下文占用；窗口来源、部分缓存覆盖保留读屏语义，详细请求统计从消息“用量”底部面板查看。自动整理、输出预留与请求计量契约保持不变，见 [专项开发方案](./docs/context_management_and_usage_design.md)。
- **计划与子任务**：计划模式、计划修订和批准状态显式呈现，批准计划不代替工具确认。子任务标明所属父任务与执行者，父任务的停止入口始终可达，不让用户填写执行后的核验结果。

## 7. 弹层、键盘与文案

- 短确认用紧凑对话框，短操作按内容展开；搜索和长列表用底部面板，常规高度不超过可用空间的 85%。
- 底部面板保留标题、关闭入口和拖柄，内容可滚动。需要确认的面板保证确认始终可达，未提交时支持返回、遮罩和下滑取消；保存中防重复提交和误离开，失败保留待保存内容。即选即存面板按第 6 节保留已保存选择。
- 工具确认展示名称、动作摘要、目标、必要参数与剩余时间；停止、拒绝、允许一次三个动作分别清晰可达，关闭不视为批准。
- 键盘只避让一次并保留安全区；短屏、横屏和大字下内容、关闭与确认均可滚动到达。
- 文案保留字段含义、错误原因和必要的删除影响，助手删除弹窗仅询问是否删除；省去教学式说明、重复口号与按钮复述。反馈不打断主要操作，错误提示不展示密钥、鉴权信息、堆栈或模型接口原始响应；用户主动展开的工具结果按第 6 节展示。

## 8. 无障碍与性能

- 正文与背景最终合成对比度至少 4.5:1；焦点、选中和错误同时具备非颜色提示。
- 图标风格统一，纯图标操作有明确名称；读屏可识别当前值、展开、选中与禁用状态，装饰不重复朗读。下拉支持方向键与取消，不唤起输入键盘。
- 减少动态效果时关闭非必要动画与模糊，保留静态状态；离屏暂停动画。无障碍导航本身不强制玻璃改为实色，玻璃降级遵循系统的减少动画设置。
- 长列表、流式输出和玻璃表面不妨碍输入、滚动与手势；性能以目标 Android 设备的 Profile 帧数据判断。

## 9. 验收要求

- **布局**：320/360dp、横屏、1.3x/2x 字号、键盘、长名称与长列表；无溢出、关键内容截断或不可达操作，逐一核对紧凑顶栏与按钮触区。
- **视觉**：实际查看深浅主题渲染，检查层级、合成对比度、触区、形状与对齐；透明表面须覆盖背后有内容的场景。
- **交互**：打开、选择、取消、提交、禁用、加载、失败重试与快速开关；验证草稿、展开选择和阅读位置，长工具输出能滚到末尾且可收起。
- **导航与辅助操作**：预测返回取消/提交、逐层关闭、侧栏跟手、读屏、键盘操作与减少动画。
- **扩展场景**：连接/安装取消和重试、运行中禁用、定义变化、环境缺失、终端大量输出与停止、计划修改及父子任务归因；操作入口只反映真实能力。
- **性能与交付**：同一设备、相同条件下比较滚动、拖动、转场与流式更新的帧耗时。分别记录自动化、视觉和真机检查；构建或截图生成不替代验收，未验证项明确列出。
