# 相月开发实施计划

更新：2026-09-24｜阶段：未发布的初版建设

本计划从当前代码继续建设扩展能力。已实现功能的入口与必须延续的行为见 [产品设计](./product_and_technical_design.md)，新增接口、数据和生命周期见 [Agent 与扩展设计](./agent_extensions_design.md)，工程与 UI 要求分别见 [AGENTS.md](../AGENTS.md)、[DESIGN.md](../DESIGN.md)。

旧 S1–S4 建设步骤及逐批修复日志收敛为下面的实现基线，历史细节可由 Git 查阅。旧 S5 对应的真机、性能和发行工作保留为 R 阶段，独立排期，不因本次文档更新自动开始发行或设备操作。

## 1. 实现基线与未验证范围

| 范围 | 当前状态 | 后续处理 |
|---|---|---|
| 四协议、服务商/模型、助手、聊天、附件、主题 | 已有实现与对应测试文件 | 保持回归，不再从零建设 |
| 单代理工具循环、文件/HTTP、三档策略、确认、停止、自动重试与恢复 | 已有实现与对应测试文件 | 扩展工具复用同一派发/记录规则 |
| Android 宿主、SAF、名单、控件、截图/手势、任务面板 | 已有实现；存在历史设备安装与用户反馈 | 补全生命周期、目标动作和性能验收 |
| 加密数据库、结构化消息、会话复制与导出 | 已有实现 | 完整分支导航、备份恢复仍是新任务 |
| 远程 MCP、动态工具来源/快照、扩展管理 | 已实现；本机真实 HTTP 四协议闭环通过 | 外部服务、真机生命周期与性能仍需独立验收 |
| Skills 本地导入、助手范围、按需读取与固定版本 | 已实现；本机四协议文件产物闭环通过 | SAF 真机导入、生命周期与性能仍需独立验收 |
| Ubuntu 安装、工作区、进程管道与 shell | 已实现；本机四协议、真机安装和进程桥通过 | 完整 UI/生命周期和性能仍需真机验收 |
| 本地 MCP stdio、合并依赖安装 | 已实现；本机 Python/Node 固件闭环与真机 stdio 探针通过 | 真实 npx/uvx 服务与依赖安装真机验收仍需独立进行 |
| 上下文预算/摘要、Plan Mode、长期记忆 | 已实现；本地四协议闭环、真实摘要连接取消与布局自动化通过 | 真机、视觉、性能与真实模型摘要质量待验收 |
| Shizuku、Termux 命令与显式递归传输 | E6 代码已接入 | 未运行测试或真机验收，见第 8 节 |
| 插件宿主、子代理等 | 尚未实现 | 按 E 阶段建设，不显示空入口 |

历史记录最近一批为 2026-09-15：记录了 644 项 Flutter 测试通过、4 项预览跳过、25 项 JVM 测试通过，以及 Profile 构建/覆盖安装。手势探针未完成实际点击验收，用户随后反馈可用；这些记录不代表当前提交已复验，也不代表真实网关或 Profile 帧性能通过。E1 本批检查另见第 3 节。

### 1.1 会话权限模式调整（2026-09-24）

- 权限独立于助手，按会话保存计划/基础/全权限三档及规划前执行档；命令和依赖安装保留两个工具、共享同一权限类别。应用工具中仅列出应用为只读，控件读取和截图归应用操作。完整矩阵见产品设计 §5.2。
- 助手只选择 MCP/Skills 和记忆范围；预览、模型定义、执行及扩展派发复检统一使用模式。空白页只保存草稿，首发建会话；批准计划原子恢复来源执行档。底部改为向上小菜单，模式保存与发送互斥，运行不串改其他会话。
- 初版 schema 9 移除助手策略 JSON，加入 MCP 启用集合及会话模式字段；最初未接入旧库转换。随后用户要求修复并安装，授权仅本次 v8 → v9 保数据迁移，范围见 AGENTS 第 5 节；不清理手机数据，不扩展历史升级链。历史 v7 → v8 升级实现/用例留在 Git 历史。
- 验证入口：`permission_mode_flow_test.dart`、`permission_mode_policy_test.dart` 及现有计划、MCP、Skills、助手、布局与加密库重开测试。自动化结果在本次交付单独记录；真机菜单、预测返回、读屏和性能尚未验收。

## 2. 顺序与依赖

| 批次 | 目标 | 前置 | 当前状态 |
|---|---|---|---|
| E1 | 工具来源/快照 + 远程 MCP 最小闭环 | 现有工具循环 | 已实现；本机真实 HTTP 闭环通过 |
| E2 | Skills 导入与按需读取 | E1 的来源与范围管理 | 已实现；本机四协议闭环通过 |
| E3 | Ubuntu PRoot、工作区、原始进程管道与 shell | 原生宿主 | 已实现；自动化、真机安装/进程桥通过，完整 UI/生命周期待验收 |
| E4 | 本地 MCP stdio、依赖安装与环境界面 | E1 + E3 | 已实现；本机 Python/Node 固件闭环与真机 stdio 探针通过 |
| E5 | 上下文预算/摘要、Plan Mode、记忆 | E1/E2；不强依赖 Linux | 已实现；本地四协议闭环、取消与布局自动化通过 |
| E5.1 | 统一 Token/缓存统计、usage 驱动预算、滚动压缩 | E5；不强依赖原生通道 | U1–U4 已接入；本机自动化与外部验收分开记录 |
| E6 | Shizuku 与 Termux 独立命令通道及递归传输 | 进程结果/取消契约稳定 | 代码已接入；自动化与真机验收未执行 |
| E7 | 声明式扩展包，再做有实际用例的插件钩子 | E1/E2/E4 | 待实现 |
| E8 | 单子代理与显式模型分派 | E5、工具归因与取消稳定 | 待实现 |
| B1–B5 | 分支、备份、文档上传、Key/成本、感知 | 各自前置见第 11 节 | 待实现 |
| R | 设备、性能、网关与发行验收 | 本次拟发行的功能闭环完成 | 单独排期 |

优先交付 E1 → E2，随后 E3 → E4。不要先制作插件市场或多代理调度框架。每批只引入所需数据与接口；仍未发布时直接同步模型/schema/生成物，不建设开发期格式兼容层。

## 3. E1：远程 MCP

设计入口：[扩展设计 §2–§3](./agent_extensions_design.md#extensions-mcp)。

- [x] 为动态工具增加稳定来源 ID、原名映射、定义修订和效果分类；保留既有 Tool/ToolExecutor 入口。
- [x] 增加 MCP 配置仓储与安全凭据引用，运行固定工具目录和连接配置；禁用立即阻止后续派发。
- [x] 实现显式 Streamable HTTP、初始化协商、JSON/SSE 响应、分页工具列表、会话关闭和请求取消。
- [x] 加入服务列表/编辑/连接检查/工具列表，以及助手范围设置；检查连接不调用业务工具。
- [x] 将文本、结构化结果和图片映射到现有回填与产物链路；大结果有界处理。
- [x] 连接失败、响应丢失、工具定义变化和超时成为可见错误，不自动重发已派发工具。

交付用例：本地可控 HTTP MCP 服务提供“读取样本文档”工具，四协议真实适配器发出调用，确认后返回并保存结果，再生成最终回答。覆盖停止时的等待响应/空闲 SSE、鉴权失效、工具目录变化与禁用；不以静态工具定义或连接成功当作闭环完成。

实现入口：[MCP 功能](../lib/features/mcp/)、[配置仓储](../lib/data/repositories/mcp_server_repository.dart)、[来源与快照](../lib/data/models/tool_source.dart)、[测试](../test/features/mcp/)。本机可控 HTTP 服务的交付用例已通过；设备与外部服务的实际验收范围见下面记录。

依赖选择：已核对 [mcp_dart 2.4.2](https://pub.dev/packages/mcp_dart/versions/2.4.2)（2026-09-03 发布、MIT、Dart ^3.4.0），具有 Streamable HTTP、AbortSignal/关闭、图片/结构化内容支持及相应测试，依赖 http/crypto/meta。本批保留已验证的 Dio 客户端；替换 SDK 时需重新验证鉴权、会话恢复、取消与“不重发已派发工具”契约。`crypto 3.0.7` 已从间接依赖改为直接依赖，用于名称/定义 SHA-256 摘要，未升级版本。

2026-09-19 E1 实现与验收记录：

- 支持 MCP `2025-06-18`、`2025-03-26`，JSON/SSE、分页、`tools/list_changed`、协议取消及 DELETE 关闭；不自动重试业务调用。每次派发前重新核对目录，确认前后和真正发送前核对权限。
- 无凭据/Bearer 与自定义请求头可配置；自定义头值全部进入安全存储，配置/快照只保存引用。更换凭据创建新引用，旧运行保持原引用；删除失败保留禁用条目供重试。地址禁止 userinfo、查询参数及片段；不跟随 HTTP 重定向。
- 响应/目录总量上限 8 MiB，目录最多 100 页/1000 个工具；模型文本预览最多 6 KiB，超限保存完整文本产物并给出引用。PNG/JPEG/WebP 复用图片回填；关闭图片能力不解码或保存图片；资源链接不自动下载。服务端只读提示不授予权限。
- 已执行格式检查、`flutter analyze --no-pub`、`flutter test --no-pub` 和 `git diff --check`；完整 Flutter 测试 810 项通过、4 项预览跳过。MCP 55 项测试包含真实 HTTP/SSE、四协议文本/结构化/图片回填、确认/撤权、定义变化、超时、等待响应/空闲 SSE 停止及最终落库。通知测试服务显式关闭 HTTP 输出缓冲，避免把尚未发送的事件误判成客户端失败。
- 工具链恢复网络/监听权限后，重新解析相同锁定依赖以消除先前可写 SDK 副本造成的分析器路径冲突，并重新生成代码。未升级依赖。SDK 候选归档、探针与测试日志均在忽略的 `build/`。
- 用户明确允许一次无损覆盖升级：schema 3 → 4 仅新增 `mcp_servers` 和工具来源可空列；假数据/假密钥测试验证会话、助手、运行、工具结果和外键保持，重开不会重复建表。例外同步记入 AGENTS 第 5 节，不扩大其他开发期兼容范围。
- Profile 构建通过（APK 约 140.2 MB），已在授权设备 `1b8418ca`（2211133C、Android 16/API 36）使用 `adb install -r` 覆盖安装，启动 `Status: ok`、冷启动 1304 ms。UI 层可访问新增 MCP 配置表单，原有助手页面正常读取。安装前加密数据库备份保存在忽略的 `build/e1_device_database_before.tar`，未卸载、清数据或更换签名。
- 装机与基础页面可访问不代表完整真机验收；外部 MCP/模型网关、Android 生命周期、预测返回和 Profile 帧性能尚未验证，未修改原生桥接。

## 4. E2：Skills

设计入口：[扩展设计 §4](./agent_extensions_design.md#extensions-skills)。

- [x] 定义 Skill 安装记录、助手启用 ID、版本/摘要和资源目录。
- [x] 实现本地目录/ZIP 导入，校验 frontmatter、包大小、路径/链接、重名；staging 安装与失败清理。
- [x] 上下文仅放名称/简述，`read_skill` 按需读取指导或明确资源；保留来源与版本。
- [x] 加入列表、详情、资源查看、启停、删除和助手选择；导入不执行脚本。
- [x] 验证活跃运行版本固定、权限收紧、资源缺失、越界引用与删除清理。

交付用例：安装“整理文档”Skill → 助手启用 → AI 按需读取 → 使用现有文件工具生成摘要产物。没有 Linux 时该文本/文件场景也可完成；脚本型 Skill 如实提示缺少环境。

实现入口：[Skills 功能](../lib/features/skills/)、[安装仓储](../lib/data/repositories/skill_repository.dart)、[版本模型](../lib/data/models/skill_installation.dart)、[回归测试](../test/features/skills/)。

2026-09-19 E2 实现与验收范围：

- 本地目录经 Android SAF 复制到临时目录，不追加持久文件工具授权；ZIP 支持根入口或单层包装目录，仅支持未加密的 Store/Deflate。安装不执行脚本，网络导入与脚本工作区副本仍分别随后续网络导入/E3 接入。
- ZIP 上限 16 MiB，解压/目录总量 32 MiB，单文件 4 MiB，最多 512 条目、16 层目录。检查实际复制/解压字节、CRC、重名、路径、链接与 frontmatter；staging 校验后切换数据库中的版本指针。只使用 name/description，其他元数据字段显示为未使用，不授予权限。
- 助手显式选择 Skill，读取按现行会话模式直接允许；上下文只注入 ID、名称和简述。`read_skill` 返回来源、内容摘要版本和相对路径，按 `nextOffset` 分段，单次完整 JSON 限 6 KiB；资源读取校验版本摘要，脚本仅可查看并说明环境缺失。
- 更新不替换旧运行版本；禁用、删除或移出助手立即阻止后续读取。删除先撤销范围，运行/查看引用释放后清理文件；提交失败还原 staging，清理失败保留重试入口，启动清理中断副本。
- 新增初版 schema 5（Skill 安装记录、助手启用 ID），同步运行快照和生成物。用户在实现后要求装机，本次仅允许已安装 E1 测试包的 v4 → v5 增量升级；只新增表/列，不改旧记录，不扩展其他开发期兼容链。假配置/假密钥的加密库测试逐表验证九张旧表的全部旧列、MCP 目录和凭据保持，重复打开及完整性/外键检查通过；安装例外同步记入 AGENTS 第 5 节。E1 的旧升级实现/用例保留在 Git 历史。
- 沿用已有 archive/file_picker/crypto；yaml 3.1.4 从间接依赖转为直接依赖，无版本升级。四协议使用可控 HTTP/SSE 模型响应，经真实适配器完成“读 Skill → 读文档 → 确认写摘要 → 保存/回填 → 最终回答”；不代表付费模型或外部网关验收。
- 已执行代码生成、桥接生成、格式检查、`flutter analyze --no-pub`、`flutter test --no-pub --timeout 60s`、`:app:testDebugUnitTest` 和 `git diff --check`：858 项 Flutter 测试通过、4 项预览跳过，31 项 JVM 测试通过。新增 48 项 Skills 功能测试覆盖导入/回滚/取消、摘要与分页、版本引用、撤权、四协议闭环和 UI，另有本次装机的 v4 → v5 升级测试；历史 E1 升级用例不再用于本次初版 schema。
- Profile APK 构建成功（约 141.4 MB）。UI 自动化覆盖 320/360dp、800×360 横屏、1x/2x 字号，以及导入预览返回、安装、查看、停用、助手草稿取消/保存；已实际查看深浅主题详情页渲染。日志和预览在忽略的 `build/e2_*`。
- 已在授权设备 `1b8418ca`（2211133C、Android 16/API 36）通过 `adb install -r` 覆盖安装，安装 `Success`、启动 `Status: ok`；Skills 列表与导入页面可访问。安装前加密数据库备份位于忽略的 `build/e2_device_database_before.tar`，安装后既有附件文件校验值保持一致；未卸载、清数据或更换签名。真实 SAF 文件复制、Activity 重建、Skills 页面真机预测返回、读屏和 Profile 帧性能仍未验收。

## 5. E3：Linux 工作区与命令

设计入口：[环境 §5](./agent_extensions_design.md#extensions-runtime)、[进程 §6](./agent_extensions_design.md#extensions-process)。分三次可验证交付，不一次铺满终端 UI。

### E3.1 运行可行性与安装

- [x] 固定首批 Ubuntu 24.04 ARM64 镜像清单、ABI、发布摘要和下载大小；明确 PRoot/loader 来源、构建方式、许可与版本。
- [x] 在 JNI 库目录打包执行文件并通过 Profile 构建；保留 compileSdk 37 / targetSdk 36。
- [x] 授权设备验证 nativeLibraryDir 启动、loader、Ubuntu shell 和基础子进程取消。
- [x] 真机验证官方下载到 ready、Pigeon 原始管道、后台取消与环境卸载保留工作区。
- [ ] 真机补全安装/文件 UI、通知停止、Activity 重建、FD 回收与性能。
- [x] 实现下载、校验、staging 解压、安装检查、取消、空间不足和失败清理。
- [x] 增加环境状态与工作区仓储，环境/依赖与用户文件分目录；重置/卸载影响明确且不自动删除工作区。

完成条件：授权设备上从未安装环境到 shell ready 可复现，固定命令退出码和文件结果正确；取消安装后原有环境/工作区仍可用。记录 APK 增量、下载量、安装及峰值空间，不能复用 Aether 的 Alpine 数字。

### E3.2 进程桥与命令工具

- [x] Pigeon 增加原始进程 API：启动、字节事件、stdin、关闭、取消及一次退出事件；按 owner/processId 归属。
- [x] 同时消费 stdout/stderr，处理 UTF-8 边界、背压、尾部输出和上限；输出产物与应用状态写入分开处理。
- [x] 实现 shell 工具、固定运行环境/工作区、确认参数、默认超时与结果保存；独立非交互进程不继承上次 cd/变量。
- [x] 整理执行宿主的命令任务类型、前后台和通知停止归属；不依赖聊天 Activity 常驻。
- [x] 取消传到底层进程组，回收子进程/FD；重启按中断失败保存，不重跑命令。

完成条件：固定脚本分别覆盖成功、非零退出、等待 stdin、无输出、持续大输出、子进程和停止；结果经真实模型适配器回填。真机验证后台/Activity 重建/服务停止，测试产物放 build/。

### E3.3 工作区体验

- [x] 首条消息自动创建会话独立工作区并注入可用命令工具，附件导入副本与产物导出；复制独立文件，删除会话联动清理且保护活动任务。
- [x] 环境安装界面展示真实阶段和清单大小；准备失败可重试。依赖管理留在 E4。
- [x] `prepare_skill` 复制运行固定版本到工作区，记录来源/版本，返回 guest 路径；安装原件保留。
- [ ] 基础管道稳定后再加入 PTY 终端、尺寸变化、Ctrl-C、会话退出和页面状态恢复；PTY 可在 E4 之后交付，不阻塞 stdio。

完成条件：会话任务生成文件后可在工作区和产物查看；切换会话不改变运行绑定，关闭终端页面不会错误终止别的任务。普通聊天不启动 Linux。

2026-09-19 E3 实现与本机验收：

- 新增初版 schema 6：环境、工作区、资源副本来源三张表，以及会话工作区绑定；运行快照固定环境安装路径与版本。用户随后要求安装新版，本次仅允许 v5 → v6 增量覆盖升级：新增上述表和可空列，原有记录不改写；不扩展其他开发期升级链。假配置/假密钥的加密库测试逐表核对十张旧表、Skills 原件与版本、MCP 目录和凭据，验证重复打开、外键/完整性及升级后的工作区读写。历史 E2 装机升级实现/用例留在 Git 历史。
- Ubuntu Base 24.04.5 ARM64 镜像、PRoot/loader 和 talloc 均固定来源与 SHA-256；构建方式、许可与实测字节数见 [Linux 资源](../third_party/linux/README.md)。官方完整镜像已通过本机实际解压，覆盖 PAX、普通文件、硬链接副本和 guest 链接。环境放入 Android noBackupFilesDir，与工作区分目录；安装采用 staging、检查后切换，失败/取消保留旧环境。
- shell 每次独立非交互进程，默认 60 秒、最大 5 分钟；stdout/stderr 每路 64 KiB 预览，合计 8 MiB 后停止，完整字节保存为产物。取消结果也保留产物引用；文本产物按 256 KiB 分页，可读到末尾且不切断 UTF-8 字符。工作区 `output/` 的变化文件自动复制为独立附件；单次最多 100 个文件 / 64 MiB，其他文件继续留在工作区。
- 原始 Pigeon 管道按 owner/processId 归属，字节块有 sequence 且等待接收确认；终态在两路尾部输出消费后交付。supervisor 用进程组、父进程死亡通知与 subreaper 回收后代。shell 的时限/输出限额从 Dart 配置传递；原始管道允许无累计时限/总输出限额，供 E4 连接使用，不开放给模型参数。命令和安装任务复用既有 FlutterEngine/服务，通知停止对应 owner，命令不要求无障碍。
- 设置只提供环境管理，工作区文件从所属会话进入；首条消息自动创建独立工作区，首轮注入就绪的 shell，文件工具不再依赖 shell 开关。复制会话复制独立文件，删除会话清理工作区与全部产物，失败可重试；活动运行租约阻止删除。文件导入/导出走显式副本与 SAF，Skill 副本按来源/修订记录。只支持 ARM64；不提供任意宿主挂载、强隔离承诺或交互终端。
- 已执行桥接/代码生成、格式检查、`flutter analyze --no-pub`、`flutter test --no-pub --timeout 60s`、`:app:testDebugUnitTest` 和 `git diff --check`：887 项 Flutter 测试通过、4 项预览跳过，31 项 JVM 测试通过；另有 4 项真实本机 supervisor 测试通过。新增 29 项工作区测试覆盖安装/取消/保留旧环境、路径与链接、Skill 固定副本、四协议真实进程闭环、停止/超时/大输出、UTF-8 分页及 UI。确认面板用例改为等待运行收口后读取文件，消除文件创建早于异步写入完成的竞态。
- Profile APK 构建通过（约 72.2 MB）；原生新增 ELF 约 292 KiB，JNI 提取策略同时改变整包压缩方式。UI 测试覆盖 320/360dp、800×360 横屏和 1x/2x 字号，实际查看了深浅主题稳定渲染。日志、镜像、渲染和测试 APK 在忽略的 `build/e3_*` / `build/linux-native/`。
- 授权设备 `1b8418ca`（2211133C、Android 16/API 36）重连后，以 Profile 包运行不启动 Dart/业务数据库的 `linux-native` instrumentation：targetSdk 36 下 JNI 目录 exec、Ubuntu shell、stdin/stdout/stderr、文件写入、退出码 23 与停止后的子进程回收通过，单次基础脚本记录为 112 ms（不作性能基准）。首轮发现 PRoot 默认临时目录不可写，显式设置 `PROOT_TMP_DIR` 后从清空的测试目录重跑通过。
- 测试前保存设备原 APK，测试和恢复全程使用 `adb install -r`；恢复后 APK SHA-256 与原包一致，启动 `Status: ok`，测试 rootfs/工作区已清理。上述临时探针没有卸载、清数据或执行 schema 升级，每次均恢复 E2 原包。PTY 按计划顺延，不阻塞 E4 原始管道接入。
- 对照 Kelivo/RikkaHub 的现代 JNI 执行与应用内解压方案，真机 Pigeon 测试补齐 stdin、stdout/stderr、非零退出与文件、超时保留输出、输出上限和退到后台后的取消/子进程回收。环境根目录规范化解决 Android 路径别名校验；宿主临时目录按进程分开。此前预解压探针在目录准备完成前自动运行，加入就绪信号后同一预解压镜像也通过，未把测试竞态归为 rootfs 链接丢失。
- 使用临时入口和内存数据库，真机经实际 `LinuxInstaller` 完成官方下载 → 校验 → 解压 → shell 检查 → ready；一次记录为 9,207 ms，文件内容 104,728,695 字节，空间采样最大增量 257,687,552 字节（约 245.8 MiB，不是精确峰值或性能基准）。卸载保留工作区测试通过，测试文件随后清理，原包恢复并核对 SHA-256。
- 2026-09-19 用户要求安装后，新增本次 v5 → v6 升级例外并完成检查，使用 `adb install -r` 将正式 E3 Profile 包覆盖到设备 `1b8418ca`：安装 `Success`、启动 `Status: ok`，环境与工作区页面可读取新表并显示未安装/空工作区。安装前加密数据库与应用文档备份在忽略且限权的 `build/e3_install/before_app_flutter.tar`；安装后 80 个已有附件/Skill 文件 SHA-256 保持。没有卸载、清数据或更换签名，手机最终保留 E3；未自动下载环境或修改助手工具策略。
- 2026-09-19 工作区归属调整：空白页不落库，首条消息创建独立工作区并在同轮注入就绪的 shell（默认 ask、尊重显式 deny）；文件工具不再要求 shell 或已安装环境。复制同步复制文件、来源记录并重映射运行快照，删除清理工作区与未登记产物，失败保留重试记录。移除手动创建/绑定入口，并修复删除当前会话时模型选择订阅的销毁竞态。此调整未改变 schema；自动化覆盖四协议首轮真实进程、会话隔离、复制/删除与失败重试、无环境文件读写、工具禁用及空白页入口，940 项 Flutter 测试通过、4 项跳过，格式/静态分析/diff 检查通过。用户随后要求装机，Profile 构建成功，核对签名一致后用 `adb install -r` 覆盖到 `1b8418ca`：安装 `Success`、冷启动 `Status: ok`，首页空白会话未显示文件入口。安装前后启动前的加密数据库、附件、Skills 与工作区共 145 个文件 SHA-256 全部一致；原 APK、限权备份与核对清单位于忽略的 `build/workspace_install/`。未卸载或清数据；设备拒绝 ADB `input tap`（缺少 `INJECT_EVENTS`），页面操作和真实首轮命令仍待真机验收。
- 同日按用户反馈移除设置页的会话工作区列表与说明，页面/入口改为“环境设置”，仅管理 Ubuntu，文件仍从会话进入。格式、静态分析、940 项 Flutter 测试（4 项跳过）和 Profile 构建通过；再次 `adb install -r` 覆盖安装与启动成功，启动前 146 个数据文件 SHA-256 全部保持，备份位于 `build/environment_settings_install/`；页面操作仍受上述设备输入权限限制。
- 完整安装/工作区 UI、通知停止、Activity 重建、FD 回收、真实 SAF、预测返回/读屏和 Profile 帧数据尚未真机验收；付费模型/外部网关未调用。

## 6. E4：本地 MCP 与依赖

设计入口：[扩展设计 §3.4](./agent_extensions_design.md#extensions-mcp)。

- [x] 添加 Node/npm、Python/uv、Git/搜索工具的按需安装，记录版本和结果，安装失败/取消保留已知状态；按用户要求三组合并为一次完整安装，设置页与模型工具共用同一白名单。
- [x] MCP 配置开放 stdio，选择已有环境/工作区及 executable/argv/cwd；敏感环境变量仅保存引用，npx/uvx 依赖预先安装并固定版本。
- [x] Dart 客户端直接连接进程原始管道，stderr 与 JSON stdout 分离，stdin 可持续写入。
- [x] 绑定服务会话与进程生命周期，关闭/取消/服务异常不会遗留进程或自动重跑调用。
- [x] 会话工具目录变化沿用 E1 的定义修订与权限规则，清理空闲后台连接。

交付用例：固定版本 Node 和 Python 各一个 MCP 服务，完成发现、确认调用、分片输出、错误、停止及退出；再选择一个实际 npm/uvx 服务做明确环境下的兼容性检查。依赖安装不是普通 tools/call 的隐藏步骤，PTY 不能替代 stdio。

实现入口：[stdio 客户端](../lib/features/mcp/mcp_stdio_client.dart)、[连接管理](../lib/features/mcp/mcp_connections.dart)、[配置模型](../lib/data/models/mcp_server_profile.dart)、[依赖安装](../lib/features/workspace/dependency_installer.dart)、[测试](../test/features/mcp/mcp_stdio_test.dart)。

2026-09-19 E4 实现与本机验收记录：

- stdio 服务以 executable + argv 直接启动，不经 shell 拼接；cwd 为 guest 路径（默认 `/workspace`），每个服务持有独立持久目录（宿主 `workspaces/mcp/<id>`，不落库，删除服务时尽力清理）。明文环境变量存 profile JSON，敏感变量只存 `SecureKeyStorage` 引用，轮换/清除语义与请求头一致；同名变量禁止明文与加密并存。命令、cwd、环境任一变化生成新定义修订并清空目录；切回 HTTP 丢弃命令。环境未就绪、目录创建失败、启动失败均映射为可见错误，不静默换环境。
- stdio 客户端复用 E1 协议骨架：握手、分页目录、定义修订与权限检查为共享实现（`McpClient` 基类），HTTP 实现改名为 `McpHttpClient` 且 55 项既有测试不变语义通过。stdout 按行承载 JSON-RPC（UTF-8 分片、`\r\n` 兼容、单帧与缓冲有 8 MiB 上限），迟到响应在超时/停止后被忽略；stderr 仅保留 4 KiB 尾部随失败消息给出，不写 AppLogger。stdin 持续写入（>60 KiB 分块）；超时与停止发送 `notifications/cancelled`；服务器 `ping` 请求应答空结果、其余拒绝。进程不设累计时限/总输出限额；连接随运行创建，运行收口、检查结束、通知停止或协议违规均终止进程组并回收任务归属，不重跑已派发调用。
- 连接检查、运行时与助手动授权共用 E1 的快照/权限路径；stdio 不读取 Bearer/请求头，环境变量在派发前解析合并。空闲清理由“连接仅存活于活动运行或检查期间”满足，不为闲置服务器保活。数据库 schema 不变（stdio 全部在 `profileJson` 内），未产生装机升级例外。
- 依赖安装合并：设置页与 `install_packages` 模型工具一次装齐 python3/pip/venv、nodejs/npm、git/ripgrep（单次 apt 命令 + 按组独立验证），失败/取消保留已安装内容与旧记录，重试重发完整安装。工具不再接受 profile 参数。
- 已执行格式检查、`flutter analyze --no-pub`、`flutter test --no-pub`（全量 1053 项通过、4 项预览跳过，其中 MCP 71 项含新增 stdio 15 项、仓储 7 项）和 `git diff --check`。stdio 测试用真实本机进程：Python（`mcp_stdio_server.py`）覆盖握手/环境透传/分块 UTF-8/RPC 错误/超时与迟到响应/停止取消通知/进程退出带 stderr 尾部/非 JSON stdout/list_changed/服务器 ping/启动器环境缺失/经 `ToolLoopHarness` 的确认调用-落库-最终回答闭环；Node（`mcp_stdio_server.mjs`）覆盖握手/调用/RPC 错误/ping。缺 python3/node 的环境对应文件自动跳过。
- 未验收范围：真实 npx/uvx 服务兼容性检查（含固定版本预装流程的实际样本）、付费模型/外部网关、Profile 帧数据。apt 合并安装未在真机复验（当时设备仅有计量移动网络，避免消耗流量；设置页入口可直接人工执行）。

2026-09-20 E4 真机验收记录（授权设备 `1b8418ca`、2211133C、Android 16/API 36）：

- 安装前备份加密数据库与应用文档（忽略且限权的 `build/e4_install/before_app_flutter.tar`，6,296 个数据文件校验值存 `before_checksums.txt`）。以忽略目录内的临时探针入口（不渲染界面、不打开业务数据库）在设备 PRoot 内用真实 `/usr/bin/python3.12` 运行 stdio 服务：握手协商 `2025-06-18`、目录发现 1 个工具、`tools/call` 回填「月相记录标记-device」（明文环境变量经 PRoot `env -i` 正确透传）、`close()` 后进程与任务归属回收、服务进程 `exit 2` 映射为带退出码与 stderr 尾部的 `processExit` 错误、探针工作区清理，全部通过（日志在 `build/e4_stdio_probe/` 与设备 cache，已清理）。
- 随后以 `adb install -r` 覆盖安装正式 E4 Profile 包（66.6 MB，SHA-256 `0d1e88a9…`），启动 `Status: ok`；覆盖后 6,296 个数据文件集合与内容除 Flutter 引擎 `files/profileInstalled` 标记外全部一致。未卸载、清数据或更换签名；schema 未变，无装机升级例外。
- 设备当前已装有 Ubuntu 环境（用户自行使用中）且 guest 内已有 python3.12/pip，故本次 stdio 验收未产生任何下载流量；探针进程与临时文件均收口清理。

2026-09-20 环境安装与本地 MCP 复查：

- 修复依赖页只保留最早 5 行输出、静默步骤不切换、stdout/stderr 分片混流和 UTF-8 解码问题。APT 显式开启管道进度、关闭 dpkg PTY、使用非交互配置及 HTTP(S) 30 秒等待超时；取消在步骤边界生效，失败/取消保留停止阶段与最新输出，原始 apt 尾部不再写入 AppLogger。Python 说明明确当前不含 uv/uvx。
- 基础环境显示准备、下载、校验、解压、配置来源、执行检查 6 步；依赖显示修复、更新索引、安装、验证 4 步，展示阶段用时、最近更新时间与真实输出，不推算整体百分比。来源探测接入取消，解压字节不再把中间 tar 与已写入文件混计。新增 `configuring` 仍使用现有环境 JSON，schema 不变。
- 本地 MCP 持有环境使用权，关闭/自然退出释放；修复启动期间关闭造成的进程遗留、运行关闭后迟到创建的连接、并发分块写入交错、stdin 阻塞时超时不返回、已完成请求误发取消通知，以及宿主停止未按取消收口。未引入新进程框架或依赖。
- 已执行代码生成、格式检查、分析、全量测试（1067 项通过、4 项预览跳过）及 diff 检查；回环 HTTP 覆盖空闲下载/来源探测取消，真实 Python/Node stdio 与可控桥接覆盖生命周期。额外以校验过的 Ubuntu amd64 基础镜像、隔离目录与本机限速软件源运行 apt 2.8.3：默认管道没有 CR 进度，修复参数产生 43 次更新，两次均 exit 0。已查看深浅主题进度渲染，布局测试覆盖 320/360dp、大字与横屏。ARM64 镜像 URL 返回 200、长度及发布摘要与清单一致；无在线 Android 设备，未装机，完整依赖安装、第三方 npx/uvx 服务与 Profile 性能不计为本次验收通过。临时复现脚本、输出与截图在忽略的 `build/install_review/`。

- 后续按用户要求在设备 `1b8418ca`（2211133C）覆盖安装本轮 Profile 包（66.6 MB，SHA-256 `53e74d0e3b363e0107f74ca89f3ceb7dac666a0fed54248e28dc34690f3b31e8`）：`adb install -r` 返回 `Success`，启动 `.MainActivity` 返回 `Status: ok`，进程存活；首次安装时间保持不变。未卸载或清数据，schema 未变；本次仅验收安装与启动，未触发环境/依赖下载或第三方 MCP 调用。记录在 `build/install_review/device/`。

2026-09-21 任务悬浮窗与用户操作交接：

- `TaskOverlay.kt` 改为左右吸附、标题拖动与小幅展开的侧边卡片；动态、公开思考、正文分栏切换，最近片段限长并节流跨桥更新。确认继续复用原执行器；结束释放执行服务，保留返回相月/关闭两个图标，不保留正文副本。普通聊天不启动服务，Linux 命令仅在已有许可时附带面板。
- 新增 `wait_for_user(prompt)`，通过 `ExecutionController` 等待与 `AgentLoop` 原有串行循环交接；运行持久化为 `awaitingUser`，同轮后续工具不提前执行。继续按运行/调用 ID 校验、清除旧界面快照；停止、权限失败、存储失败及中断恢复不伪造用户完成或重放调用。沿用文本状态列，未改表结构或 schema 版本。
- 已执行 Pigeon / Riverpod 生成、格式检查、`flutter analyze`、全量 `flutter test`（1108 项通过、4 项预览跳过）、`:app:testDebugUnitTest`（37 项通过）和 `git diff --check`。新增回归覆盖真实工具循环等待/继续/停止/落库、迟到和重复继续、重启不重放、桥接编解码、独立公开思考/正文及相月内 320dp / 2x 字号深浅主题布局；JVM 覆盖吸附边界、展开锚点、终态保留与关闭状态。
- 功能实现阶段未装机；原生悬浮窗视觉、拖动手感、键盘/横竖屏、跨应用交接、系统服务生命周期与 Profile 性能仍待真机验收，不以 JVM 或 Flutter 布局测试代替。
- 随后按用户要求，在设备 `1b8418ca`（2211133C）用 `adb install -r` 覆盖安装本轮 Profile 包（66.6 MB，SHA-256 `55dd8118aec4ddc2e7be1b9cc8688e5c04a1265f6f574445c9f46e6a2eb4e45c`）：安装返回 `Success`，启动返回 `Status: ok`，进程存活，无障碍服务已重新挂接。首次安装时间保持不变，未卸载、清数据或更改 schema；仅验证安装与启动，未触发模型调用或用户操作交接。记录在忽略的 `build/task-panel-install/`。

同日根据装机反馈调整悬浮窗：

- 收起缩至约 168dp，展开约 264dp；取消三个分页，改成限高 136dp 的最近消息列表，按稳定消息/Part/工具调用 ID 原位更新，保留最近 6 项。工具执行后的模型等待阶段保留“执行了 xxx”或实际失败/拒绝文案。
- 使用同一原生窗口做可中断的尺寸、透明度、箭头与吸附过渡，状态文字和新消息轻入场；跟随系统关闭动画设置，截图/生命周期隐藏立即取消动画。返回图标改为左箭头，返回与关闭均先清除本次面板状态，切回原 App 不重现。
- 已重新生成桥接与 Riverpod 代码，通过格式检查、静态分析、全量 Flutter 测试（1111 项通过、4 项预览跳过）、JVM 测试（40 项通过）与 diff 检查。回归覆盖消息顺序、完成快照替换、列表限长、工具结果文案保留、返回后旧状态丢弃及几何动画中断起点；实现阶段未再次装机，原生动画手感与帧性能尚未真机验收。
- 随后按用户要求，在设备 `1b8418ca`（2211133C）用 `adb install -r` 覆盖安装二次调整后的 Profile 包（66.6 MB，SHA-256 `61a127e528875e7ce789ccf15552249441d7780cbc7b419d610b425cfd6f4316`）：安装 `Success`、启动 `Status: ok`，进程存活、无障碍服务重新挂接，首次安装时间不变。未卸载或清数据；仅验证安装与启动，未触发模型任务或将动画效果计为验收通过。记录在忽略的 `build/task-panel-v2-install/`。
- 后续修正收起态正文展示：直接取最新正文原文，按实际宽度/字号显示末尾两行，流式增量与完成快照原位替换，不逐段重复切换动画；确认、用户交接和工具结果仍按实际阶段显示。仅调整原生展示与 UI 契约，`:app:testDebugUnitTest` 43 项及 diff 检查通过；此次未重新装机，原生换行与流式观感尚待真机验证。

## 7. E5：Agent 上下文与计划

设计入口：[扩展设计 §8](./agent_extensions_design.md#extensions-planning)。

- [x] **预算先行**：提取 ContextBuilder，计算工具定义/系统提示/消息预算，预留输出并保留完整工具组；超限有明确结果。
- [x] **分支摘要**：保存覆盖范围和来源，切分支后失效；摘要请求可停止，用量独立，失败不删除历史或无限重试。
- [x] **Plan Mode**：只开放宿主确认的只读工具，版本化计划卡、编辑/批准/取消；批准后创建关联执行运行，具体动作继续确认。
- [x] **记忆**：助手/全局范围、来源、编辑/删除和关键字检索；显式写入按会话模式授权，不从工具结果自动提炼长期事实。

交付用例：长会话在预算内保持任务约束和工具结果；计划阶段拒绝通用 shell/未知效果 MCP；修改计划后旧批准失效；记忆可查、改、删且不会被摘要重新写回。四项可分批，不等记忆完成才交付预算管理。

2026-09-22 E5 实现与本机验证（预算默认值按 2026-09-24 目录集成同步）：

- `chat/context/` 提供预算选择与独立摘要请求；`chat/planning/` 提供宿主只读限制和版本化计划卡；`features/memory/` 提供有来源的检索、显式写入与管理。运行仍由同一 `AgentLoop` 推进。
- 预算使用配置窗口，未配置时按 models.dev 目录、本地默认 128000 依次解析，输出优先采用协议实际参数，未提供时按目录输出上限、本地 4096 依次预留，另留协议余量。80% 阈值尝试摘要；初版仅压缩较早的纯助手叙述，用户原文、完整工具组、当前任务及上一组完整交互仍保留。因此超长文档/动作历史可能仍超限，会保存明确失败，不截掉当前任务或悄改模型参数。
- 摘要保存覆盖 ID、分支锚点、内容指纹、算法版本、来源模型与独立 API 用量。分支/原文变化不复用；同一运行同一来源失败不自动重试。真实 HTTP 的等待响应和空闲 SSE 均验证停止断连、不发后续聊天请求；摘要不会创建记忆。
- 四协议经真实适配器与本机 HTTP/SSE 固件验证“计划提交 → 批准新运行 → 确认记忆写入 → 检索回填”。另覆盖 shell/仿冒只读/MCP 拒绝、旧修订批准失效、取消、不同分支拒绝、复制归属、已提交计划的中断恢复、显式轮次预算、记忆范围收紧、中文长条目完整回填与删除后迟到编辑。
- UI 自动化覆盖 360dp 深浅主题、320dp/2x 字号和横屏/2x 字号的模式切换、计划编辑/批准与记忆取消/保存/编辑/删除；不等同于视觉、读屏、预测返回真机或 Profile 帧验收。
- schema 7 新增摘要、计划修订、记忆表与助手记忆范围。2026-09-22 用户要求提交、推送并装机后，仅为本次已有 E4 安装加入 v6 → v7 增量升级；十三张旧表的旧列、MCP/Skill/工作区、凭据和文件保持，重复打开及完整性/外键检查由 `e5_upgrade_test.dart` 验证。其他旧 schema 仍拒绝升级，不扩展历史兼容链。设备操作使用备份与 `adb install -r`，实际安装结果另行记录，不将本机检查视为装机成功。
- 自动化入口：`test/features/chat/context/`、`test/features/chat/planning/`、`test/features/memory/`。`dart format`、`flutter analyze`、全量 `flutter test`（1166 通过，4 项预览跳过，含本次 v6 → v7 安装例外）、`git diff --check` 与 Profile APK 构建通过（66.8 MB）。没有真实付费网关或真机验收。

### 7.1 E5.1：上下文计量与用量增强

详细设计、数据契约与验收矩阵见 [专项开发方案](./context_management_and_usage_design.md)。本批记录独立于上面的 E5 历史验收。

- [x] U1：四协议用量归一化、请求尝试记录、缓存读写与完整性；同步初版 schema/生成物，避免重复计量。
- [x] U2：有效 usage 基准、请求投影估算、协议实际输出预留与上下文失效规则。
- [x] U3：请求/运行/会话统计、缓存命中率、上下文与用量 UI；保留未知与部分覆盖状态。
- [x] U4：滚动摘要、单次长任务切分、历史按需读取、完整动作组保护和一次受限超窗恢复；同步保护规则。
- [ ] U5：真实模型摘要质量、Profile 真机视觉/交互/性能和外部网关验收，须在实际授权范围内另行安排。

本批没有新增依赖、原生桥接或另一套 Agent 循环。schema 8 新增请求表、旧用量档案和检查点元数据，移除重复 usage 列。当时安装仅允许 v7 → v8，其他旧库拒绝；该次升级实现/用例现保留在 Git 历史，旧用量原样保留但不伪装为新请求。历史 E5 v6 → v7 升级实现/用例留在 Git 历史，`initial_state_reopen_test.dart` 只验证当前初版重开。

本地回归覆盖四协议真实 HTTP/SSE → 控制器 → 数据库的逐尝试计量，未知/异常/修订、停止与继承记录；验证单条用户任务中连续至少三次滚动压缩、分段候选失败、激活事务回滚、来源失效和有界历史分页。外部动作、MCP、shell、拒绝/失败和图片采用完整原文保护，超大受保护组仍可能明确超限。不会自动写长期记忆或宣称任意任务无限续跑。

2026-09-22 本机验证：代码生成、格式检查、`flutter analyze`、全量 `flutter test`（1211 通过，4 项已有预览跳过）、`git diff --check` 通过。布局覆盖 320/360dp、横屏、1x/2x 字号，既有发送/停止/键盘回归继续通过；没有新一轮视觉、读屏、真实摘要质量或 Profile 真机验收。

随后按用户要求提交并推送功能提交 `bf92b8a` 到 `origin/main`。用户再次明确要求安装，授权本次 v7 → v8 保数据升级：先保存原 APK 与应用文件备份，凭据不导出，只核对加密偏好文件摘要。以同签名 Profile 验证包经 `adb install -r` 在设备 `1b8418ca` 升级，16 张旧业务表除归档用量列外的原列摘要全部一致（含 73 个会话、1346 条消息、281 个附件、129 次运行、578 条工具记录），582 份旧用量报告原样归档；新请求表为空，完整性、外键及重开检查通过。306 个非数据库文件和偏好文件摘要一致，仅 Flutter 的 `files/profileInstalled` 标记变化；没有卸载或清数据。

最终产品 Profile 包（75.4 MB，SHA-256 `7b8eaccc70f25ba76426b627afc89791dbe69879a54f482bb170b63fd0cab72c`）随后再次以 `adb install -r` 覆盖：安装 `Success`、冷启动 `Status: ok`（2941 ms），进程存活，设备 APK 哈希与构建产物一致，偏好文件仍不变，未见启动异常标记。安装例外补丁通过代码生成、格式、静态分析、全量 Flutter 测试（1213 通过，4 项已有预览跳过）及 diff 检查。本次只验证数据升级和安装/启动，不宣称完成真机交互、视觉、性能或真实模型验收；备份及脱敏核对结果在忽略的 `build/context_install/`。

## 8. E6：系统命令通道

实现契约与调研依据见 [系统命令专项设计](./system_command_channels_design.md)。2026-09-25 用户确认并列通道、全局启用和显式目录递归传输后接入：

- [x] Shizuku 状态/授权入口、shell UID UserService、PFD 输出、native supervisor 与调用归属。
- [x] Termux RUN_COMMAND、版本化原生 runner 初始化、严格 PendingIntent 回调、私有日志分块、停止和宿主租约。
- [x] 运行快照、三档权限和任务通知共用既有宿主；不要求无障碍，不自动更换身份/重发命令。
- [x] 显式文件/目录传输、限量清单、摘要校验、逐文件提交、部分完成与来源/产物记录；无自动同步或远端删除联动。
- [ ] 实际 Shizuku/Termux 安装、授权、撤权、输出、子进程/FD 回收、后台和宿主死亡验收。
- [ ] 递归传输边界、四协议模型闭环、界面/视觉/性能验收。

本批没有改动或执行测试；不把编译或安装启动当作上述功能验收。schema 不变，不引入装机升级例外。现有 `flutter analyze --no-pub` 仍报告测试文件中的两项 `prefer_single_quotes`，未修改任务外测试。默认格式检查另发现已有 `test/support/schema8_fixture.dart` 待格式化，未改动；`flutter analyze --no-pub lib` 无问题，native/Kotlin 编译及 Profile APK 构建通过。命令沿用当前不设总时限的契约，连接期限与租约独立。

2026-09-25 用户要求装机后，以 `adb -s 1b8418ca install -r` 覆盖安装 E6 Profile 包：安装 `Success`，启动 `Status: ok`、`LaunchState: WARM`、`TotalTime: 855 ms`，进程存活，未见本进程 AndroidRuntime/flutter 错误。设备 APK 与构建产物 SHA-256 一致（`7773c16d05eb07fdd28c0f6b49eed750d512a2a9635334f59726dbee408669f9`）。原 APK 与应用数据备份位于忽略且限权的 `build/e6_install/20260925_194928/`；安装前后、启动前的 `app_flutter`、`shared_prefs`、`files` 和会话工作区归档逐字节一致。没有卸载、清数据或更换签名，没有开启外部通道或执行命令；本次仅验证覆盖安装、上述数据保留与启动，不代替 E6 功能、冷启动或性能验收。

每条通道单独完成实际验收。Termux 双向 MCP 服务桥、Root、PTY 和插件不混入本批。

### 8.1 会话主环境与文件后端（2026-09-25）

按用户最终要求，Ubuntu / Termux 只在环境设置中全局选择，新会话首次发送时固定绑定，创建后不可切换；聊天输入框恢复原布局，不保留环境入口或草稿。命令、文件和 Skill 共用绑定；两侧目录独立保留，AI 与文件页显式复制；Shizuku 独立启用，Termux 依赖自行管理，MCP stdio 仍固定 Ubuntu 服务目录。

- 已接入全局选择/新会话固定绑定/运行快照、统一 shell、WorkspaceFileAccess、Termux 类型化原生文件 helper、分页与条件写入、文件页预览/复制、两侧会话目录复制/删除及失败重试归属。
- schema 10 维护初版契约与生成物；用户确认装机后，仅增加 9 → 10 保数据升级，事务核对各表数量、旧来源内容、默认 Ubuntu、外键及完整性；其他版本拒绝升级。不提交、推送，不卸载或清数据。
- Pigeon/Riverpod/Drift 生成、lib 静态分析、native C++ / Kotlin 编译及 diff 检查通过。全量 analyze 有 3 处测试替身接口待同步及 2 项既有 lint；格式检查仍报告既有 schema8_fixture。按约定未修改测试，不能宣称全量检查通过。
- 尚未编写或执行测试，通道功能尚未真机验证。待验收：无 Ubuntu 的 Termux 文件/命令/Skill 闭环、计划档只读、全局选择与已有会话固定绑定、双目录复制、部分失败、停止/撤权、复制删除重试、窄屏大字与预测返回。编译和安装启动不替代上述验收。

## 9. E7：扩展包与插件钩子

设计入口：[扩展设计 §9](./agent_extensions_design.md#extensions-plugins)。

- [ ] 先实现 manifest + Skills/MCP/resources 的声明包，以及来源、依赖、能力差异预览。
- [ ] 安装不执行，启用不自动加入所有助手；升级 staging、运行版本固定，卸载处理所属进程和共享文件。
- [ ] 在实际插件用例提出后加入 Node 插件宿主，仅开放工具注册、受限上下文补充和结束通知。
- [ ] 定义输入/输出、次序、时限、取消与异常归属；宿主校验不能被插件替换，UI 只渲染已有 Part。

完成条件：一个“文档整理”声明包可安装、启用、运行、升级与卸载；程序化钩子另以正常、超时、崩溃、非法返回样本验证。不把支持 manifest 宣称为已兼容 Pi/ToolPkg，不同时维护 Node 与 QuickJS 两套宿主。

## 10. E8：单子代理

设计入口：[扩展设计 §10](./agent_extensions_design.md#extensions-subagent)。

- [ ] 定义子代理配置与 parentRunId/agentId，复用同一 AgentLoop、Provider 和 ToolExecutor。
- [ ] 实现 delegate_task、独立上下文、一个活跃子任务、结果/产物与来源展示。
- [ ] 父子预算和工具权限取交集，父停止传到子网络、确认、原生进程；父等待时让出设备队列。
- [ ] 用户配置子任务模型，创建子运行时固定，错误不自动换模型或再次委派。

完成条件：父任务委派一份独立文档调查并使用结果继续；拒绝、部分成功、失败、进程结束和取消都能正确归因与保存。并发/fork 不在本批。

## 11. 可单独排期的产品扩展

设计入口：[扩展设计 §11](./agent_extensions_design.md#extensions-product)。下列任务不因扩展主线完成自动标记完成。

| 批次 | 具体交付 | 前置与验收重点 |
|---|---|---|
| B1 分支导航 | 同级回答切换、分支浏览、当前位置恢复 | 直接用当前消息树；运行分支不可被查看操作改写；工具历史不重放 |
| B2 加密备份 | 数据范围预览、口令加密包、认证后恢复、关联重映射 | 先定备份格式/KDF/AEAD 依赖；快照前停止活动任务；错误口令/损坏/空间不足/中断不破坏原库 |
| B3 原生文档上传 | 按协议上传、文件 ID 缓存、过期/清理与进度取消 | 先选一个真实支持文件输入的协议样本；本地抽取保持显式选择；跨端点不复用 ID |
| B4 凭据与成本 | 多 Key 的显式选择规则、按任务选模型、价格与用量汇总 | 只安全存储凭据；真实请求逐次计数；不因切 Key 重做工具；未知费用不伪装精确 |
| B5 通知感知 | 按 App 监听、观察回填、RemoteInput 确认回复 | 真机授权/撤权、过期通知和取消验收；既有截图/前台窗口能力不重建 |

MCP OAuth、resources/prompts、旧 SSE 与网络 Skill 导入分别以实际样本立项，验收范围见扩展设计 §12.1；不混入 E1 的连接成功承诺。

后续 Root、更多子代理并发、Code Mode、定时事件、模板/角色卡与远程访问，按 [扩展设计 §12](./agent_extensions_design.md#extensions-later) 的进入条件立项，暂不预建框架。

## 12. R：发行前仍需完成

此清单保留未完成项，不把历史构建/用户反馈升级为完整验收记录。

- [ ] 四协议真实网关：聊天、公开思考、工具、图片和停止分别记录模型、端点类型与脱敏结果。
- [ ] 两条基础闭环：文档摘要保存；固定测试 App 搜索并打开详情，再检查实际目标 App。
- [ ] Android 生命周期：离开/返回、Activity 重建、服务停止、进程终止、权限撤销、锁屏和应用名单收紧。
- [ ] 视觉动作：实际截图尺寸、坐标命中、原生浮层、动作成功但观察失败；不能用未完成的探针代替。
- [ ] DESIGN 布局与交互：320/360dp、横屏、1.3x/2x 字号、键盘、长名称/长输出、深浅主题、预测返回。
- [ ] Profile 帧数据：聊天阅读、流式更新、侧栏、任务/终端面板；记录设备、条件及 UI/raster 帧。
- [ ] 数据与发行：备份排除规则、合并 Manifest、正式签名、联网、安装启动与正常覆盖升级；当前 Release 仍使用 debug 签名。
- [ ] 发布范围与 GitHub Releases 包对应，只声明本次实际验证能力；Profile 不作为正式发行包。

## 13. 检查与进度维护

工程命令统一使用 AGENTS 第 6 节，不在这里复制另一份工具链版本或安装流程。每批交付记录源码提交、实际执行的检查、样本/设备、结果与未验证项。仅依赖变化时 pub get，生成输入变化时重新生成；原生变化执行 JVM 检查，真机验收使用 Profile，更新安装只用 adb install -r。

文档改动只检查事实、相对链接/锚点、任务依赖、命令和 git diff --check，无需构建或装机。后续完成一批时更新本计划状态及产品现状表，保留一个简短验收记录，不再累积重复的历史行为说明。
